export const frontendHtml = (user: { email?: string } | null) => `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Infinity Agents</title><style>
body{margin:0;min-height:100vh;background:#0b0d10;color:#f2f4f7;font:16px system-ui,sans-serif}main{width:min(880px,calc(100% - 32px));margin:auto;padding:48px 0}.panel{border:1px solid #252a33;border-radius:16px;padding:20px;background:#11141a}textarea{width:100%;min-height:132px;background:#0b0d10;color:inherit;padding:12px;box-sizing:border-box}button,a{background:#90e56a;color:#10230a;border:0;border-radius:10px;padding:10px 16px;font-weight:700;text-decoration:none;cursor:pointer}.output{min-height:180px;white-space:pre-wrap;background:#080a0d;padding:16px;margin-top:20px}header{display:flex;justify-content:space-between;align-items:center;margin-bottom:28px}.muted{color:#98a2b3}</style></head><body><main>
<header><h1>Infinity Agents</h1>${user ? `<span>${escapeHtml(user.email ?? "已登录")} · <a href="/logout">退出</a></span>` : ""}</header>
${user ? `<section class="panel"><textarea id="prompt" placeholder="输入你的问题…"></textarea><p><button id="send">发送</button></p><div id="status" class="muted"></div><pre id="output" class="output"></pre></section><script>
const p=document.querySelector('#prompt'),s=document.querySelector('#send'),o=document.querySelector('#output'),t=document.querySelector('#status');s.onclick=async()=>{if(!p.value.trim())return;s.disabled=true;o.textContent='';t.textContent='模型正在响应…';try{const r=await fetch('/v1/chat/completions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({messages:[{role:'user',content:p.value}],stream:true})});if(!r.ok||!r.body)throw Error(await r.text());const reader=r.body.getReader(),d=new TextDecoder();let b='';for(;;){const {done,value}=await reader.read();b+=d.decode(value||new Uint8Array(),{stream:!done});const es=b.split('\\n\\n');b=es.pop()||'';for(const e of es){if(!e.startsWith('data:'))continue;const x=e.slice(5).trim();if(!x||x==='[DONE]')continue;const j=JSON.parse(x),delta=j.choices?.[0]?.delta?.content;if(delta)o.textContent+=delta}if(done)break}t.textContent='完成'}catch(e){t.textContent='请求失败：'+e.message}finally{s.disabled=false}};</script>` : `<section class="panel"><h2>使用 ZhangYvJing 登录</h2><p class="muted">Infinity Agents 仅向已登录的第一方账户开放。</p><p><a href="/login">登录</a></p></section>`}
</main></body></html>`;
function escapeHtml(value: string): string { return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;"); }
