# C0 secret scan

Command:

```text
git grep -nE '(sk-[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|wc_[A-Za-z0-9]{32,})' \
  -- ':!package-lock.json'
```

Result: no matches in tracked files. Environment files, Cloudflare secrets,
remote D1, zhangbot Redis, and Docker runtime memory are outside this static
repository scan and were not queried.
