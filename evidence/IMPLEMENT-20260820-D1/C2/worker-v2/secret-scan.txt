# C2 secret scan

No credential values were added. Worker credentials are generated at request
time and only their SHA-256 plus encrypted recovery copy are stored in D1.
The static tracked-file key/credential pattern scan returned no matches.
