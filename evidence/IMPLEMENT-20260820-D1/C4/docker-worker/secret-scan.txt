# C4 secret scan

Static tracked-source scan used:

```text
git grep -nE '(sk-[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|wc_[A-Za-z0-9]{32,})' -- ':!package-lock.json'
```

Result: no production credential, provider key, D1 token, R2 parent key,
Redis password, or Relay secret was added to source control. Example files use
placeholders only. The Docker image history contains the Claude Code version
and non-secret runtime defaults, not Worker/Relay/provider credentials.

Remote environment variables, local ignored env files, Docker process memory,
remote D1, R2, and zhangbot Redis were not queried by this static scan.
