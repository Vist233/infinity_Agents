Modified implementation: `backend/code_agent/worker/consumer_v2.py` initializes the advisory Relay
hint list before each loop, retries transient control-plane transport failures, and reconnects after
a stale Worker session. A credential error or duplicate active instance remains fatal.

Added regression coverage: `tests/test_worker_consumer_v2.py` simulates a failing Relay and a
transient control-plane/TLS failure, asserting fallback polling and session reconnect instead of a
process crash.

Updated records: `HANDOFF.md`, `evidence/IMPLEMENT-20260820-D1/C5/checkpoint.md`, and
`worker.cloudflare.env.example` now record the D1-only SQL boundary, Relay ACL issue, local result,
and the current GHCR multi-architecture digest.
