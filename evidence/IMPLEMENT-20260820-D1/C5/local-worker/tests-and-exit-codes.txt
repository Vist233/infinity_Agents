`docker build --progress=plain -t infinity-agent-worker:local-v2 -f backend/Dockerfile.worker .` -> exit 0

Published image verification: `ghcr.io/vist233/infinity-agent-worker:v1` -> multi-architecture
manifest `sha256:b0e0dcced7ddc0cc58e314e4aa49985a2a85cff7882ae69f7725d0febdf33e40`

`pytest -q tests/test_worker_consumer_v2.py tests/test_worker_v2_http_client.py tests/test_worker_v2_executor.py tests/test_redis_relay.py` -> exit 0; 7 passed

`docker inspect infinity-agent-worker-b-v2 --format 'status={{.State.Status}} restarts={{.RestartCount}}'` -> `status=running restarts=0`

Local container logs observed:

- `POST /api/worker/v2/connect` -> HTTP 200
- `POST /api/worker/v2/heartbeat` -> HTTP 200
- `POST /api/worker/v2/poll` -> HTTP 200 (repeated)
- Relay `GET /v1/hints` -> HTTP 503; process continued polling
- One control-plane TLS EOF before the fix caused a restart; after the retry/reconnect fix the
  replacement container remained `running` with `restarts=0` through the observation window.

Read-only D1 session query observed the local Worker instance with `session_epoch=3`, a future
`lease_expires_at`, and `disconnected_at=NULL`.

`npx wrangler d1 execute ... --remote` -> exit 0; no rows were changed.
