# Secret scan — C5 / real-case2-20260820

Command scope: tracked files, excluding lockfiles and source maps.

High-confidence credential/private-key patterns (`sk-ant-*`, long `sk-*`, AWS access keys,
GitHub tokens and PEM private-key headers): 0 matches.

Worker and provider values in tracked example/config files are placeholders only; no filled
credential file was added to the repository. The scan output was redacted and no secret value was
copied into this evidence directory.
