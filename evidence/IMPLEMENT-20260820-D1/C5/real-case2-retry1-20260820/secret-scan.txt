Command:
`git diff --check && ! rg -n -i '(anthropic_api_key|authorization:[[:space:]]*bearer|credential[[:space:]]*=[[:alnum:]_-]{16,}|redis_url=.*@|password[[:space:]]*=[^[:space:]]+)' evidence/IMPLEMENT-20260820-D1/C5/real-case2-retry1-20260820`

Result: exit 0. No credential value, Authorization header, API key, Redis URL, or password pattern was found.
