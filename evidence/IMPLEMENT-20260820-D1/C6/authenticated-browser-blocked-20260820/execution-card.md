# Execution Card C6 — authenticated Task Center browser acceptance

唯一可观察结果：在真实登录的 Infinity Agents 会话中，验证三入口导航、Task Center、真实 Task、
Artifact 下载、登录态和响应式布局；并将自动化客户端无法读取页面时准确记录为未通过。

不创建 preview Task、不调用 Worker v1、不写入 D1/R2，不更改线上数据。
