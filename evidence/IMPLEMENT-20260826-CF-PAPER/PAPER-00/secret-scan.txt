# PAPER-00 secret scan

Command:

`matches=$(rg -l --hidden -g '!node_modules/**' -g '!*.lock' -g '!evidence/**' -g '!backend/__pycache__/**' '(sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{20,}|github_pat_[A-Za-z0-9_]{20,}|xox[baprs]-[A-Za-z0-9-]{20,}|AKIA[0-9A-Z]{16}|-----BEGIN [A-Z ]+ PRIVATE KEY-----)' cloudflare-worker/src frontend/lib backend/code_agent backend/Dockerfile.worker 2>/dev/null || true); if [ -n "$matches" ]; then echo "$matches"; exit 1; else echo 'No high-confidence secret material found in active Edge/frontend/worker paths.'; fi`

Result: exit code 0. No high-confidence secret material found in active Edge/frontend/worker paths.

The scan was path-only and did not print token values. No token material was written to this evidence.
