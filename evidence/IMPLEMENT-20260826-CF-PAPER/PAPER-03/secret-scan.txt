# PAPER-03 secret scan

Command:

`matches=$(rg -l --hidden -g '!node_modules/**' -g '!*.lock' -g '!backend/__pycache__/**' '(sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{20,}|github_pat_[A-Za-z0-9_]{20,}|xox[baprs]-[A-Za-z0-9-]{20,}|AKIA[0-9A-Z]{16}|-----BEGIN [A-Z ]+ PRIVATE KEY-----)' cloudflare-worker/src cloudflare-worker/test frontend/lib backend/code_agent backend/Dockerfile.worker evidence/IMPLEMENT-20260826-CF-PAPER/PAPER-03 2>/dev/null || true); if [ -n "$matches" ]; then echo "$matches"; exit 1; else echo 'No high-confidence secret material found in PAPER-03 active/test/evidence paths.'; fi`

Result: exit code 0. No high-confidence secret material found in PAPER-03 active/test/evidence paths.

The scan reports paths only and did not print token values.
