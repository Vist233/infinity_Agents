PAPER-10 local release baseline
date=2026-08-27
branch=cloudflare-deploy
baseline_commit=455ae849c572aa285cc752a10e21fd69f031b18d
release_commit=61cc66d509a86ac93cebef9fd955644d68d278c0
previous_checkpoint=evidence/IMPLEMENT-20260826-CF-PAPER/PAPER-09.5/checkpoint.md

The worktree was clean before the PAPER-10 runtime-contract changes. The
candidate commit contains the reviewed Processor runtime code, tests, delivery
definition, systemd unit, dependency lock, Edge non-secret Processor ID, and
design/runbook updates. The delivery definition pins the exact source commit
and SHA-256 values for the Processor source, lock file, and service unit.

The previous Cloudflare-managed preflight remains preserved in the earlier
history of this card. Its facts were accurate at that time: the old runtime
profile and OCI digest were not specified, and no external write was made.
The user has since explicitly approved the single-host `zhangbot` runtime and
provided its read-only feasibility findings. That decision replaces the old
runtime selection only; it does not make a production preflight pass.

Source-control state before final evidence commit:

- `HEAD` was `455ae849c572aa285cc752a10e21fd69f031b18d`.
- `git status --short --branch` showed `cloudflare-deploy` with only the
  intended delivery-definition update and evidence changes pending.
- Local `origin/cloudflare-deploy` tracked `825ba4024ea1c248c18c72d8b950453d8dd9d8ed`, which did not match the candidate.
- A read-only `git ls-remote --heads origin cloudflare-deploy` could not
  contact GitHub because the configured proxy `127.0.0.1:7897` was unreachable;
  no remote-ref claim is made until a later read-only verification succeeds.

No Cloudflare account/API credential, D1/R2 parent credential, Redis
credential, Processor token, provider key, or Secret value was read, written,
or persisted in this local phase.
