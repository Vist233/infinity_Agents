PAPER-10 local release baseline
date=2026-08-27
branch=cloudflare-deploy
baseline_commit=455ae849c572aa285cc752a10e21fd69f031b18d
release_commit=61cc66d509a86ac93cebef9fd955644d68d278c0
previous_checkpoint=evidence/IMPLEMENT-20260826-CF-PAPER/PAPER-09.5/checkpoint.md

The worktree was clean before the PAPER-10 runtime-contract changes. The
candidate commit contains the reviewed Processor runtime code, tests, delivery
definition, systemd unit, dependency lock, Edge non-secret Processor ID, and
design/runbook updates. The delivery definition pins the exact source commit
and SHA-256 values for the Processor source, lock file, and service unit.

The previous Cloudflare-managed preflight remains preserved in the earlier
history of this card. Its facts were accurate at that time: the old runtime
profile and OCI digest were not specified, and no external write was made.
The user has since explicitly approved the single-host `zhangbot` runtime and
provided its read-only feasibility findings. That decision replaces the old
runtime selection only; it does not make a production preflight pass.

Source-control state before final evidence commit:

- `HEAD` was `455ae849c572aa285cc752a10e21fd69f031b18d`.
- `git status --short --branch` showed `cloudflare-deploy` with only the
  intended delivery-definition update and evidence changes pending.
- Local `origin/cloudflare-deploy` tracked `825ba4024ea1c248c18c72d8b950453d8dd9d8ed`, which did not match the candidate.
- A read-only `git ls-remote --heads origin cloudflare-deploy` could not
  contact GitHub because the configured proxy `127.0.0.1:7897` was unreachable;
  no remote-ref claim is made until a later read-only verification succeeds.

No Cloudflare account/API credential, D1/R2 parent credential, Redis
credential, Processor token, provider key, or Secret value was read, written,
or persisted in this local phase.

2026-08-28 post-start failure and remediation baseline

- The first start of release `61cc66d509a86ac93cebef9fd955644d68d278c0`
  reached systemd `activating/auto-restart` with no main process. Read-only
  journal diagnosis reported `218/CAPABILITIES` while spawning Python.
- Disposable user-manager probes identified `PrivateDevices` and
  `ProtectKernelTunables`/`ProtectKernelModules`/`ProtectControlGroups` as
  incompatible on this host; the compatible minimum-hardening probe passed.
- The targeted rollback removed only the attempted Processor secret, token,
  unit, current symlink, and release. D1 migrations and metadata were
  preserved; R2, Redis, Relay, and Cloudflared were not changed.
- The checked-in service, delivery hash, runbook, design note, and regression
  test now record this host constraint. The next release must be rebuilt from
  the resulting immutable commit; the old release must not be redeployed.

2026-08-28 fixed internal endpoint protocol baseline

- This local-only subphase started from clean `cloudflare-deploy` at
  `5ca83c60c6247e3271a639544c4233a791ef7860`; the local tracking ref
  `origin/cloudflare-deploy` matched that commit at start.
- The sole objective was to replace dynamic Processor URL paths with the four
  fixed internal paths in the subphase card. No Cloudflare rule, secret,
  token, deployment, D1/R2 object, zhangbot service, Redis, Relay, or
  Cloudflared write was authorized or performed.
- No unrelated user modification was present at baseline. The only new
  untracked artifact at start was the scoped subphase record itself.

2026-08-28 WAF capability and immutable-artifact preflight baseline

- The subphase began from clean `cloudflare-deploy` at
  `84edb2cd34919ecb42d3ea7af7f1704c471adc21`; the local tracking ref matched
  that commit and no unrelated worktree change was present.
- Scope was limited to the authorized fixed-endpoint WAF capability check and
  the subsequent PAPER-10 immutable-release preflight. No D1 migration,
  R2-object, Edge deployment, Secret, zhangbot, Redis, Relay, or Cloudflared
  write was in scope before the release identity passed.
- The owner-provided WAF token metadata check passed with mode `0600` and the
  current owner. The token value was used only through an stdin header pipe;
  it was never printed, persisted, hashed, or included in evidence.

2026-08-28 manifest-drift remediation baseline

- The local remediation began from clean commit
  `c73e780b6c9dff342a6dfbbe8f3164ac2b4db520` on `cloudflare-deploy`; a
  read-only `ls-remote` confirmed the exact same SHA at
  `origin/cloudflare-deploy`.
- The scoped change is limited to the Processor delivery manifest's source
  aggregate hash and a regression test that fixes the four source-file inputs,
  the `sha256sum` line format, and the second SHA-256 aggregation step.
- The WAF rule/entrypoint from the preceding attempt was verified absent before
  this local change. No Cloudflare, D1, R2, Secret, zhangbot, Redis, Relay, or
  Cloudflared write is part of this remediation baseline.

2026-08-28 immutable preflight / dependency-install failure baseline

- External work began only after review commit
  `793dec74a5ca6717e08e9083f673d648622b4095` was pushed and read back on
  `origin/cloudflare-deploy`; the working tree was clean at the preflight.
- The exact WAF rule/entrypoint was created and read back, then the shared
  secret and one zhangbot token were delivered through secure channels. The
  Processor release archive matched locally and remotely, with no `._*` files.
- The first attempted release install failed while downloading the pinned
  `pypdf` dependency: the remote package-host read timed out and pip exited
  `2`. No release directory, current symlink, unit registration, Processor
  process, or R2 object was created by that failed install.
- This is a failure baseline, not a successful deployment baseline. The
  exact WAF rule, Edge secret, zhangbot token, and transfer archive were
  removed by targeted rollback; D1 migrations/metadata and existing
  Redis/Relay/Cloudflared were preserved.

2026-08-28 dependency-closure retry baseline

- The retry started from clean branch `cloudflare-deploy`, commit
  `6c67c8ec94eb32f8e9a77013c49c34f6c374b8d3`. Before local closure edits,
  the target account/Worker/D1/R2 and zhangbot runtime were read-only checked;
  D1 `0017`–`0021` were already applied and were not rerun.
- The source archive was the reviewed commit archive with SHA-256
  `5d694b88f413abe59c64c4276d49d4e924dad05bb272348807ecccf7006e174b`.
  The portable dependency archive was independently rebuilt with SHA-256
  `241dce8114d95fe6a74ca86eb745dfe5687e8d58302e015d0d539bfcbf609b17` and
  contains the exact wheel inputs for the pinned Processor requirements,
  including `typing_extensions==4.13.2` for the next retry.
- The corrected release attempt failed at offline dependency resolution
  because the prior lock omitted pypdf's Python 3.10 `typing_extensions`
  requirement. The exact WAF/secret/token changes made before that step were
  rolled back; no Processor release/current/unit/process or R2 object exists.
- The current local candidate changes are limited to the zhangbot dependency
  lock, its delivery manifest hash, and one delivery regression assertion.

2026-08-28 secret-handoff retry baseline

- The backed-up candidate was `343738798619546e313a0030f7b7391a6d32cec1` on
  `cloudflare-deploy`, with a clean worktree before the retry. Read-only
  Cloudflare and zhangbot preflight passed; D1 `0017`–`0021` were not rerun.
- The exact WAF rule was created/read back, then the Edge secret was written
  and a Processor token handoff was attempted. The stdin wiring was invalid,
  so the handoff was rejected as unsafe despite a non-authoritative shape
  check. The secret/token were revoked immediately; no Processor release or
  service was started.

2026-08-28 artifact-verifier retry baseline

- The current backed-up baseline was `3039acc5cc3b169ecb5d0c4b2090e980b35ee95d`
  on `cloudflare-deploy`, clean before the retry. Exact WAF/secret/token
  setup had passed with no Processor release yet.
- A local archive verification script failed before transfer due to an
  invalid no-match exception expression. The WAF rule, Edge secret, and
  zhangbot token were immediately revoked; no release/current/unit/process,
  Edge deployment, D1/R2 write, or existing-service change remained.

2026-08-29 GitHub backup blocker baseline

- The clean local baseline is commit
  `d67952b5bf560aebfbbf0671fab145fcb66718a6` on `cloudflare-deploy`.
  The last remote ref confirmed before the network failure is
  `3039acc5cc3b169ecb5d0c4b2090e980b35ee95d`.
- Direct and proxied GitHub connectivity failed; no new remote ref was
  observed. No Cloudflare or zhangbot write was attempted after the artifact
  verifier rollback.

## 2026-08-29 release retry baseline and rollback

- Starting baseline: clean `cloudflare-deploy`, local and remote
  `7f1944a6e056e469509e1eecf5f7df88b5358a12`.
- The exact current source, dependency, and unit hashes matched the checked-in
  delivery definition. The WAF token was mode `0600`; the target zone and
  fixed endpoints were rechecked read-only before the rule write.
- This retry used only the existing production account/Worker/D1/R2 targets,
  the approved `zhangbot` host, and the existing fixed-IP rule contract. It
  did not rerun D1 migrations or alter Redis/Relay/Cloudflared.
- The release script path assertion failed before unit registration. All
  capabilities created in this retry were removed and read back absent.

## 2026-08-29 corrected smoke-check retry

- The fresh release baseline was backed-up commit
  `a270b30ad4d144aad5431713d163dee8e7fcac4b`; source/lock/unit hashes and
  remote archive checks matched.
- Venv creation and offline dependency installation completed, but the
  quote-unsafe import smoke check failed before systemd registration. The
  release and all newly created capabilities were removed and verified absent.

## 2026-08-29 listener assertion retry baseline

- The fresh backed-up baseline was `6a70062186b68b2cd4703f762d3a8f62d3b6eb1e`.
  Release installation, import, and systemd start completed; the only stop
  was an over-broad host-listener assertion that misclassified existing
  cloudflared metrics. The Processor PID had no listener.
- All capabilities from this retry were removed and read back absent; no
  existing service or data resource was changed.

## 2026-08-29 GitHub backup confirmation and release retry baseline

- The clean branch is `cloudflare-deploy`; local HEAD and the exact
  `origin/cloudflare-deploy` SSH `ls-remote` readback are
  `becde2db27aa7ef7e31bfb2ddbe4a0f4ce7be8cf`. The non-force backup push
  returned `up to date`.
- The approved Cloudflare account, Worker, D1 database, R2 bucket, fixed
  zhangbot egress, and current artifact/hash inputs were rechecked read-only.
  Remote D1 reported no migrations to apply; migrations were not rerun.
- The retry used the existing exact WAF/secret/token/release targets only.
  It stopped after systemd user-service registration with exit `1`; rollback
  left no Paper Processor release/current/unit/process or token, removed the
  exact WAF capability and Edge Secret, and preserved D1/R2 plus existing
  Redis/Relay/Cloudflared.

## 2026-08-29 wheel archive metadata retry baseline

- The starting backed-up commit was
  `4647fcb761708d89794f63f99baf5317ed215c6d`; branch and worktree were clean.
  Local Processor source, lock, unit, and manifest hashes matched.
- The exact WAF rule and one secret/token pair were created only after
  read-only preflight. Source/wheel transfer reached zhangbot staging, but
  remote wheel metadata validation exited `32`; the staging and all newly
  created capabilities were removed.
- A local replacement wheel archive was rebuilt with metadata disabled and
  explicit `ustar` members; it has not yet been accepted remotely or used for
  a release. D1/R2 and existing services remain unchanged.

## 2026-08-29 systemd verification-order retry baseline

- The clean backed-up starting commit was
  `7505d07012ef1e210b7782eff5b6484e0e68d778`. Current source/lock/unit hashes
  and the metadata-free remote staging archive matched the delivery contract.
- The exact WAF rule and one stdin-only secret/token pair were created after
  preflight. Release extraction and offline dependency installation passed;
  the unit verification order stopped before unit installation because the
  unit references the not-yet-activated `current` symlink.
- The exact release/staging/capabilities were rolled back. No D1 migration,
  R2 write, Edge deploy, or existing-service change occurred.

## 2026-08-29 live acceptance browser-blocked baseline

- The clean backed-up commit was
  `5d2c12875296bd7ce1fad824cb122fc44dab76a1`; the Processor release and Edge
  deployment were built from its matching artifact hashes.
- The live release reached Edge version `4d2a792b-a767-4e91-8a66-09aac0c673e9`
  at 100% and one active Processor instance, but authenticated browser
  navigation produced no DOM. The release was therefore rolled back to Edge
  version `d287b02d-a94c-4caa-b473-70f2368f4999`.
- WAF/Secret/token/release/service state is absent after rollback. D1/R2 and
  existing Redis/Relay/Cloudflared remain preserved/active; no live paper
  data was created by this attempt.

## 2026-08-29 Kimi text-gate / WAF-active release baseline

- The clean local baseline for this continuation is
  `794f8b10d0c86c50a0575cc4fe2869aaa0aa537b` on `cloudflare-deploy`. The
  actual read-only remote ref is `e551a5994cd228f19a2ae816c4529e4b04cf41a1`;
  no GitHub write is in this subphase. The local evidence commit contains no
  production source change.
- Kimi mainland text acceptance is recorded separately as exact visible
  `KIMI_MAINLAND_TEXT_PROBE_OK` against Worker version
  `79755db3-c12d-4737-b601-aa99f11e3f93`. The exact current WAF rule is active
  and read back under entrypoint
  `65e15547ea3144feb70791fc155d1df0` / rule
  `5695e7eb000c4f49b77a616cff1411ae`.
- The fresh read-only preflight verified artifact hashes, target account/
  Worker/D1/R2, no pending migrations, unchanged R2 metadata, zhangbot
  venv/ensurepip/pip, fixed egress/source checks, no stale Processor state,
  and unchanged existing Redis/Relay/Cloudflared services. D1 migrations
  `0017`–`0021` remain applied and are not to be rerun.
- External state at this baseline is exactly the corrected Edge deployment and
  the newly active exact WAF rule. Edge shared secret, zhangbot token/release/
  service, R2 object, and Paper live acceptance remain pending.

## 2026-08-29 secret-handoff retry rollback baseline

- The retry began after a fresh immutable preflight and exact WAF readback. It
  created only the new Edge shared-secret binding before the SSH token step.
- The token handoff wrapper exited `1` before writing `processor.env` because
  `set -e` treated EOF on the no-newline stdin value as fatal. The exact WAF
  entrypoint/rule, Edge Secret, zhangbot staging, and local temporary secret
  were then removed and read back absent. No Processor release/service or data
  object was created.
- This failure is retained as a wrapper diagnosis. It does not change the
  reviewed delivery contract or require a migration/data rollback.

## 2026-08-29 fresh immutable preflight after Kimi mainland text PASS

- The clean local baseline for this retry is `1fc350e7a80c5e330492e235641dea477eb88bee`
  on `cloudflare-deploy`. The actual read-only remote ref is
  `e551a5994cd228f19a2ae816c4529e4b04cf41a1`; no GitHub write was performed.
  The fixed-endpoint source, dependency-lock, and service-unit hashes all
  match `backend/paper_processor/delivery.v1.json`, and no tracked `._*`
  path exists.
- The corrected mainland Kimi result remains the exact authenticated visible
  `KIMI_MAINLAND_TEXT_PROBE_OK` result against Worker version
  `79755db3-c12d-4737-b601-aa99f11e3f93`. Read-only version inspection also
  confirmed the current secret-triggered 100% version retains
  `MODEL_BASE_URL=https://api.moonshot.cn/v1` and `MODEL_ID=kimi-k2.6`;
  only the secret name was inspected.
- Cloudflare read-only checks matched account `3cfba3bb2ec69798aa4881b05d80810f`,
  zone `a6954af7cee9fcecb610d087bdce3e01`, Worker `infinity-agents-edge`, D1
  `infinity-agents-db` (`9ee9ec94-cb42-40b5-8372-681c7b57c105`), and R2
  `infinity-agents-resources`. D1 reported no migrations to apply and the
  schema query reported zero writes; R2 remained 15 objects/41.9 MB; health
  was HTTP 200 with `paper_processor=unconfigured`; the Paper shared-secret
  name was absent.
- The scoped WAF token metadata was mode `0600`; token verification was
  active, target Rulesets read was HTTP 200 with four non-custom rulesets,
  and the custom entrypoint read was the expected HTTP 404/not-found after
  the prior rollback. No WAF rule is active at this baseline.
- zhangbot read-only checks passed for Ubuntu 22.04/x86_64, Python 3.10.12,
  installed/candidate `python3.10-venv` `3.10.12-1~22.04.17`, venv help,
  disposable venv creation, ensurepip, and pip. Redis, Redis Relay, and
  Cloudflared remained active; the Processor unit, token env, current symlink,
  actual runner process, and public Processor listener were absent; Docker was
  absent. Four successful independent egress checks returned
  `39.105.204.121`; two additional endpoints timed out/failed and were not
  used as passes. arXiv and PMC source checks returned HTTP 200.
- An inert stale directory from a prior failed delivery was found under the
  release root. After confirming no current symlink, unit, or runner process,
  it was moved—not deleted—to the exact 0700 quarantine path
  `/home/zhangyvjing/.local/share/infinity-paper-processor/quarantine/455ae849c572aa285cc752a10e21fd69f031b18d`;
  the formal `releases/` root was read back empty. It will not be reused.

## 2026-08-29 exact WAF rule recreated after fresh preflight

- The first non-secret WAF create payload was rejected HTTP `400`/API error
  `20127`; immediate readback remained HTTP `404` and no partial state existed.
  A corrected inline-set expression was then submitted only after rechecking
  the entrypoint precondition.
- The exact rule now reads back as entrypoint
  `6a212d8fb2444135a6b2511e7d8ad8d0` / rule
  `a7f6a28a87624da28d595a11eeb5d92b`, enabled and logged, action `skip`,
  products `bic` only, with the four fixed method/path pairs and source/host
  scope recorded in `waf-current-rule.md`. No other WAF rule was changed.
- External state is now limited to that exact WAF rule; Edge shared secret,
  Processor token/release/service, R2 object, and live Paper acceptance remain
  pending. The next write is the paired secret/token handoff.

## 2026-08-29 Edge/zhangbot capability handoff

- The first local secret-generation attempt was stopped before any external
  write because the generated file included a newline and measured 65 bytes.
  The corrected generation produced exactly 64 hex bytes in a mode-0600
  temporary file; the value was never printed or recorded.
- The authorized Edge `PAPER_PROCESSOR_SHARED_SECRET` write exited `0` and
  name-only readback exited `0` with the binding present. The same value was
  delivered once over SSH stdin to zhangbot; remote read returned EOF status
  `1` with a non-empty token, which the EOF-tolerant handoff intentionally
  accepts. The remote file readback passed mode `600`, one line, one key, and
  64-hex shape; the local temporary value was removed successfully.
- Edge `/health` read back HTTP `200` with D1/R2 configured and
  `paper_processor=configured`. zhangbot post-read showed the token shape
  PASS, Processor still inactive/no runner, and existing Redis/Relay/
  Cloudflared active with unchanged listener inventory. No R2 object, D1
  migration, or Edge code deployment occurred in this handoff step.
- External state at this baseline is the exact WAF rule plus the paired Edge
  secret and zhangbot token. The next step is transfer/validation and
  installation of the current commit-named Processor release.

## 2026-08-29 archive-precondition stop and capability-first rollback

- The release transfer did not start because the local package directory was
  named for the prior evidence commit `5e42e4e…`, while the clean current HEAD
  was `5c4b7563ec29fed62a8a5a63fc8c45186461f1b7`; the expected current-HEAD
  archive paths were absent. No archive was transferred and no release/service
  was installed.
- The rollback removed the active WAF rule/entrypoint and read back the
  entrypoint as HTTP `404`; removed the zhangbot token and staging path; and
  attempted to delete the Edge shared secret. The first delete returned exit
  `1` because this Wrangler version does not support `--yes`; a supported
  confirmation-input retry returned exit `0`, and name-only readback confirmed
  the binding absent. No secret value was recorded.
- D1/R2 metadata, Kimi provider configuration, and existing Redis/Relay/
  Cloudflared services were preserved. The next retry must package the exact
  current HEAD and repeat the immutable preflight before recreating WAF and
  capability material.

## 2026-08-29 current-HEAD artifact rebuild and read-only preflight

- Evidence rollback commit `63cb12c2cdf3f3704e2a76e7f33f7ce367ac248d` is a
  clean `cloudflare-deploy` HEAD. Its exact source archive and offline wheel
  archive were rebuilt; manifest hashes, lock hashes, required members, and
  no-`._*` checks passed. No transfer or external write occurred.
- Read-only Cloudflare checks identify account
  `3cfba3bb2ec69798aa4881b05d80810f`, Worker `infinity-agents-edge`, D1
  `9ee9ec94-cb42-40b5-8372-681c7b57c105`, R2
  `infinity-agents-resources`, and current 100% deployment version
  `5b7c9f4b…` with mainland Kimi configuration. D1 is fully migrated with
  zero-write schema metadata, R2 is unchanged at 15 objects/41.9 MB, the
  Paper secret is absent, WAF entrypoint is absent, and health is 200 with
  Processor unconfigured.
- Read-only zhangbot checks confirm the exact installed `python3.10-venv`
  package, working venv/ensurepip/pip, preserved existing services/listeners,
  fixed egress/source access, and no active Paper Processor state. This is a
  ready-to-write preflight only; D1 migrations remain untouched.

## 2026-08-29 exact WAF capability recreate

- After the fresh preflight, the exact fixed-endpoint BIC-only zone rule was
  created and immediately read back. Entrypoint `4eee604b…` and rule
  `08b7fc2e…` are enabled/logged and match only `39.105.204.121`, the fixed
  Worker host, POST connect/poll/control, and PUT object.
- The rule skips only BIC; no wildcard or broader security bypass is present.
  No D1/R2/zhangbot/Redis/Relay/Cloudflared write occurred in this step. The
  next capability write is the paired Edge secret/token handoff, with WAF
  deletion/readback first on failure.

## 2026-08-29 Processor install hash-check rollback

- The exact current package transferred and its remote archive SHA/member
  checks passed. The install script then falsely rejected the source aggregate
  because it hashed absolute path text instead of the delivery definition's
  relative path text; lock and unit identity checks passed and installation
  stopped before venv/service creation.
- WAF, Edge secret, zhangbot token, release/current/unit/staging were all
  rolled back successfully. No D1/R2/Edge code/Redis/Relay/Cloudflared
  change occurred. The retry will use the corrected relative-path check only.

## 2026-08-29 fresh preflight after relative-path correction

- Clean baseline `9e5de68d…` was repackaged with the exact commit-named source
  and wheel archives. Source/lock/unit identity, locked wheels, required
  members, and no-AppleDouble checks passed. No source or migration changed.
- Read-only Cloudflare checks pass for the current mainland Kimi deployment,
  exact D1/R2 target, no pending migration/zero-write schema, unchanged R2,
  absent Paper secret/WAF entrypoint, and 200 health with Processor
  unconfigured. zhangbot checks pass for the exact venv package, fixed
  egress/source access, preserved existing services, and no Processor state.
- This is a write-ready preflight only; D1 migrations remain untouched.

## 2026-08-29 transfer command rollback

- The current archives reached zhangbot staging, but SSH environment/host
  ordering prevented remote validation (`hostname contains invalid characters`,
  exit `255`). No release/service installation ran.
- WAF/Edge/zhangbot capability and staging rollback completed with safe
  absence readbacks; Redis/Relay/Cloudflared, D1, R2, and Edge code were
  preserved. Retry is limited to command ordering plus fresh preflight.

## 2026-08-29 latest rollback and mainland provider gate

- The authenticated mainland Kimi probe returned the exact requested marker
  against Worker version `79755db3-c12d-4737-b601-aa99f11e3f93`; provider
  credentials remain excluded from evidence.
- The latest install guard failed only because `rg` is absent on zhangbot
  (`127`); no Processor release was installed. WAF/Edge Paper secret and all
  new zhangbot paths were read back absent after rollback, while existing
  services remained active. This is not a clean deployment baseline yet; a
  fresh read-only preflight is required.

## 2026-08-29 current clean release preflight

- Local implementation source is unchanged at HEAD `9e5de68d…` on
  `cloudflare-deploy`; only evidence edits are uncommitted. The exact archive
  pair and manifest identity checks pass. Read-only GitHub ref is
  `e551a599…`.
- Cloudflare target/account, active mainland Kimi Worker, D1 applied schema,
  unchanged R2, absent Paper capability/WAF, and health all read back as
  expected. zhangbot exact Python runtime/venv prerequisites, fixed egress,
  source access, existing service preservation, and no Processor state pass.
- The real authenticated Kimi mainland text marker is a separate exact PASS;
  the Processor release and remaining PAPER-10 end-to-end acceptance are still
  pending. No external write occurred in this preflight.

## 2026-08-29 Processor transfer rollback baseline

- The latest transfer stopped before remote validation due to SSH argument
  ordering, not artifact content; the new WAF/Edge secret/zhangbot token and
  staging were removed and read back absent. Existing host services, D1, R2,
  and Edge code were preserved.
- The corrected next baseline keeps the same HEAD/archive hashes and changes
  only the host-first SSH invocation. PAPER-10 remains incomplete.

## 2026-08-29 retry preflight baseline after corrected rollback

- WAF, Edge Paper secret, and zhangbot Processor token/state are absent after
  the last transfer rollback; existing Redis/Relay/Cloudflared remain active.
  Current Worker remains mainland Kimi and D1/R2 are unchanged.
- Current HEAD/archive identity is unchanged and the corrected read-only
  package/egress checks pass. The next write is a newly created exact WAF rule;
  no deleted capability ID will be reused.

## 2026-08-29 archive-hash guard rollback

- Host-first transfer reached remote validation but stopped before extraction
  because archive SHA and source aggregate SHA were supplied to the wrong
  variables. Wheel SHA matched. WAF/secret/token/staging cleanup and absence
  readbacks passed; existing services, D1/R2, and Edge code were preserved.
- The next attempt changes only the hash variable names/inputs and repeats a
  fresh preflight. PAPER-10 remains incomplete.

## 2026-08-29 post-install readback diagnosis and Kimi mainland reconciliation

- The real authenticated browser probe remains a precise mainland Kimi text
  PASS: Worker version `79755db3-c12d-4737-b601-aa99f11e3f93` rendered exactly
  `KIMI_MAINLAND_TEXT_PROBE_OK` for the exact requested prompt. No credential
  value is recorded and StepFun is not used.
- The latest marked zhangbot install passed archive/root/hash/manifest,
  wheel extraction, Python 3.10 venv, ensurepip, offline locked pip, imports,
  install record, unit install, daemon reload, enable, and start. Its
  post-start readback then returned `1`; the wrapper performed WAF-first
  rollback (rule HTTP `200`, entrypoint HTTP `204`), Edge Paper secret
  deletion, and host cleanup. No D1 migration, R2 object, or Edge code deploy
  occurred.
- A read-only follow-up found no Processor token, unit, current symlink,
  commit release, or runner; the staging parent was present but empty. Redis,
  Relay, Cloudflared, and their listeners remained unchanged. Today's journal
  contains only the start/stop pair for this attempt; an older journal entry
  contains the already-recorded 403/1010 browser-signature failure and is not
  attributed to the latest start.
- Current Cloudflare readback is clean: Worker version
  `dc1e6477-4f5c-4e6a-b61d-331620719be2` is 100% with mainland Kimi .cn /
  `kimi-k2.6` and fixed Processor bindings, the Paper secret name is absent,
  health is `200`/`paper_processor=unconfigured`, D1 schema read has zero
  writes, R2 remains APAC/15 objects/41.9 MB, and the custom WAF entrypoint is
  absent. PAPER-10 remains incomplete; the next retry adds explicit
  post-start status diagnostics only.

## 2026-08-29 capability handoff shape rejection and rollback

- After a fresh exact WAF rule/readback, Edge shared-secret creation and
  name-only readback returned `0`. The zhangbot stdin handoff correctly
  rejected the temporary env file because the source stream's final newline
  produced `line_count=2` (the token value shape was valid and mode was
  `600`); no production token env file was installed and no Processor was
  started.
- Capability-first rollback passed: WAF rule/entrypoint deletion returned
  HTTP `200`/`204` with final absence, Edge secret deletion returned
  `0`, and host cleanup removed the temporary token material. Read-only
  state shows no Processor token/runner/service and no active WAF entrypoint;
  existing Redis/Relay/Cloudflared remain active. The handoff retry must
  strip transport-only trailing newlines before the one-line env-file check.

## 2026-08-29 browser-control blocker final baseline

- Mainland Kimi text gate is independently PASS with the exact visible marker
  on Worker version `79755db3-c12d-4737-b601-aa99f11e3f93`; no credential
  value is recorded and StepFun is not used.
- The zhangbot-only Processor release, fixed BIC-only WAF rule, Edge shared
  secret, and Edge version `0c6a212f-b442-4872-b182-e32ccd0702e3` were
  created and read back through non-browser readiness, but required
  authenticated browser DOM control was unavailable because the tab belonged
  to the coordinating session. All new capabilities/release/service were
  rolled back, and Edge returned to `dc1e6477-4f5c-4e6a-b61d-331620719be2`.
- Final D1/R2 and existing-host readbacks are unchanged and safe. PAPER-10
  is blocked only at the real browser acceptance gate; no PASS is claimed.

## 2026-08-30 root-cause audit baseline

- Branch/worktree: `cloudflare-deploy`, clean; local HEAD
  `154f9e16ddeffcccc2398dbbdf545497ed065bec`. Read-only
  `git ls-remote --heads origin cloudflare-deploy` returned the exact same
  SHA and ref.
- Scope was read-only diagnosis of the real paper turn observed between
  00:37:49 and 00:41:46 CST. No browser, code, deployment, D1/R2 write,
  provider change, or host write was performed.
- D1 read-only metadata proved one turn with five `search_paper` calls, one
  `materialize_paper` call, six succeeded tool results, and no
  `read_paper`/`analyze_paper_image` call. The associated paper resource and
  Processor attempt both completed successfully; details are in
  `root-cause-audit-20260830.md`.
