# PAPER-10 Kimi K2.6 mainland endpoint correction deployment

Date: 2026-08-29
Branch: cloudflare-deploy
Local review commit: 286d0c177f5ebc89b740709bcb3f466c493d83ce

## Authorized external operation

- Dry-run command: `npx wrangler deploy --dry-run --message
  'PAPER-10 Kimi K2.6 mainland endpoint correction'`; exit `0`.
- Deploy command: `npm run deploy -- --message 'PAPER-10 Kimi K2.6 mainland
  endpoint correction'`; exit `0`.
- Target: Cloudflare Worker `infinity-agents-edge`, custom domain
  `infinity.zhangyvjing.com`.
- New Worker version: `79755db3-c12d-4737-b601-aa99f11e3f93`.
- Deployment readback: deployment
  `a98fd1bd-ff46-4768-8cd8-af23dcc53fca`, version percentage `100`.
- Version readback: version number `151`, annotation
  `PAPER-10 Kimi K2.6 mainland endpoint correction`.
- Read-only `/health` readback: HTTP `200`, D1 and R2 configured, Paper
  Processor unconfigured.

The deployed non-secret provider variables are the reviewed mainland contract:
`MODEL_BASE_URL=https://api.moonshot.cn/v1` and `MODEL_ID=kimi-k2.6`.
The model credential was not read, changed, printed, or recorded.

## Acceptance stop

The existing authenticated Chrome tab was rediscovered by its current visible
URL/title (`https://infinity.zhangyvjing.com/` / `Infinity Agents`) on each
attempt. Three claim attempts were refused because the tab remained owned by
the source browser session. No DOM, cookie, storage, login data, or provider
request was read or sent. Therefore no `.cn` text-probe result is claimed,
and no Paper search, image, tool, D1, R2, or Processor acceptance was run.

PAPER-10 remains blocked on a controllable authenticated DOM. The corrected
Worker version is intentionally left at 100% so the next verified probe tests
the corrected endpoint; no StepFun rollback was performed.

## External-state boundary

This subphase changed only the authorized Edge Worker deployment. D1
migrations were not rerun; no D1 row, R2 object, WAF rule, Edge shared secret,
zhangbot token/release/service, Redis/Relay/Cloudflared state, or GitHub ref
was changed.

## 2026-08-29 text gate and WAF capability update

- Coordinator-provided real authenticated text-gate result against the
  corrected Worker version `79755db3-c12d-4737-b601-aa99f11e3f93`: exact
  prompt `请只回复：KIMI_MAINLAND_TEXT_PROBE_OK`; exact visible result
  `KIMI_MAINLAND_TEXT_PROBE_OK`.
- The prior international `.ai` 401 is superseded for this provider gate.
  Kimi K2.6 remains at 100%; StepFun was not used.
- The scoped WAF capability preflight then created and read back the exact
  fixed-endpoint BIC-only rule: entrypoint
  `65e15547ea3144feb70791fc155d1df0`, rule
  `5695e7eb000c4f49b77a616cff1411ae`, action `skip`, products `bic`, enabled
  and logged. The expression is recorded in `waf-current-rule.md`.
- This is not a PAPER-10 completion record. No Edge shared secret, zhangbot
  Processor token/release/service, R2 object, or D1 migration write has
  followed yet; D1 migrations remain applied and are not to be rerun.

## 2026-08-29 secret-handoff failure and rollback

- The Edge shared-secret write succeeded, but the matching SSH token handoff
  stopped before writing because the remote wrapper treated EOF on the
  newline-free stdin value as fatal. No Processor release or service started.
- Capability-first rollback removed the current WAF rule/entrypoint and read it
  back absent, deleted the Edge Secret, removed zhangbot staging, and removed
  the local temporary secret. This is a stopped attempt, not a deployment
  success; D1/R2 and existing Redis/Relay/Cloudflared were preserved.

## 2026-08-29 fresh preflight and stale-release isolation

- The current retry began from clean local HEAD
  `1fc350e7a80c5e330492e235641dea477eb88bee`. The real authenticated mainland
  Kimi text gate remains a precise PASS against Worker version
  `79755db3-c12d-4737-b601-aa99f11e3f93`; no provider secret value was read or
  recorded. The current secret-triggered 100% version was read back with the
  mainland `.cn` endpoint and `kimi-k2.6` model.
- Read-only Cloudflare/D1/R2/WAF checks passed with D1 migrations untouched,
  R2 unchanged at 15 objects/41.9 MB, no Paper shared secret, and no custom
  WAF entrypoint after rollback. Read-only zhangbot Python, service, listener,
  no-runner, fixed-egress, and source checks passed.
- An inert old release directory that was outside the active service path was
  moved, not deleted, to the exact 0700 quarantine path recorded in
  `baseline.txt`. The formal release root was read back empty. This was
  preparatory cleanup only; no Processor release, service, R2 object, D1 row,
  Edge secret, or Edge deployment was created in this retry.
- PAPER-10 remains incomplete. The next write is the fresh exact WAF rule and
  immediate readback; `deployment.txt` must not be interpreted as a PASS
  deployment record.

## 2026-08-29 exact WAF rule retry

- The first non-secret WAF payload was rejected HTTP `400`/error `20127`; the
  immediate entrypoint read remained `404`, with no partial state. The corrected
  request then created and read back entrypoint
  `6a212d8fb2444135a6b2511e7d8ad8d0` and rule
  `a7f6a28a87624da28d595a11eeb5d92b`.
- Readback validation passed for one enabled/logged zone custom rule, action
  `skip`, only BIC, exact fixed host/source/four method/path pairs, and no
  wildcard. This is not a full deployment or PAPER-10 PASS: no Edge shared
  secret, Processor token/release/service, R2 object, or full live acceptance
  has followed. The next action is the paired secret/token handoff.

## 2026-08-29 archive-precondition stop and rollback

- The release transfer stopped before `scp`: the local archive was named for
  prior HEAD `5e42e4e…`, while current HEAD was `5c4b7563…`. No release or
  Processor service was created.
- Capability-first rollback removed the WAF rule/entrypoint and verified 404,
  removed zhangbot token/staging, and deleted the Edge shared secret after a
  supported confirmation-input retry. The first `--yes` attempt was a CLI
  option error and was not treated as deletion.
- D1/R2, Kimi, and existing Redis/Relay/Cloudflared were preserved. This is a
  failed pre-install attempt, not a deployment or PAPER-10 PASS.

## 2026-08-29 Edge/zhangbot capability handoff

- The first local generator check stopped before external mutation at a
  newline-included 65-byte value. The corrected one-time 64-hex value was
  written to the Edge `PAPER_PROCESSOR_SHARED_SECRET` binding and delivered
  once to zhangbot via secure stdin. Edge name-only and zhangbot value-free
  metadata readbacks passed; the local temporary value was removed.
- Edge health is HTTP `200` with `paper_processor=configured`. No Processor
  release/service, R2 object, D1 migration, or Edge code deployment has
  followed. This remains a pre-install capability record, not a deployment or
  PAPER-10 PASS.

## 2026-08-29 current-HEAD artifact and preflight record

- No deployment or external write occurred in this subphase. The clean
  current HEAD `63cb12c2…` was packaged and validated locally, including the
  source/wheel archive hashes, pinned dependencies, required members, and
  no-AppleDouble checks.
- Read-only preflight confirmed the current 100% Worker remains on mainland
  Kimi (`https://api.moonshot.cn/v1`, `kimi-k2.6`), D1 `0017`–`0021` are
  applied and were not rerun, R2 remains 15 objects/41.9 MB, and health is
  200 with the Paper Processor unconfigured. The WAF entrypoint and Paper
  shared secret are absent.
- zhangbot read-only checks confirmed the exact `python3.10-venv` package,
  working venv/ensurepip/pip, unchanged existing services/listeners, and no
  Processor unit/release/process/token. This record authorizes only the next
  exact WAF create/readback, not a PAPER-10 completion claim.

## 2026-08-29 exact WAF capability record

- The exact fixed-endpoint BIC-only WAF rule was created and read back with
  HTTP `200` and semantic validation exit `0`: entrypoint `4eee604b…`, rule
  `08b7fc2e…`. It is the only new external change in this phase.
- No Edge shared secret, zhangbot token/release/service, D1 migration, R2
  object, or existing service change has followed. The next operation is the
  paired secret/token handoff, and any failure must remove this WAF rule first.

## 2026-08-29 Processor install hash-check failure and rollback

- The commit-named archive transfer and remote SHA/member checks passed. The
  install stopped before venv creation because its absolute-path aggregate did
  not match the relative-path manifest hash; lock/unit hashes matched.
- Capability-first rollback removed the WAF rule/entrypoint, Edge Paper secret,
  zhangbot token, and all new release/service/staging paths. No deployment,
  D1 migration, R2 object, or existing-service change occurred.
- This is a failed installer attempt, not a deployment or PAPER-10 PASS. The
  retry uses the manifest-relative hash command and a fresh preflight.
