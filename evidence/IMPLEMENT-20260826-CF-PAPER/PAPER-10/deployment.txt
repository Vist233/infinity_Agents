# PAPER-10 Kimi K2.6 mainland endpoint correction deployment

Date: 2026-08-29
Branch: cloudflare-deploy
Local review commit: 286d0c177f5ebc89b740709bcb3f466c493d83ce

## Authorized external operation

- Dry-run command: `npx wrangler deploy --dry-run --message
  'PAPER-10 Kimi K2.6 mainland endpoint correction'`; exit `0`.
- Deploy command: `npm run deploy -- --message 'PAPER-10 Kimi K2.6 mainland
  endpoint correction'`; exit `0`.
- Target: Cloudflare Worker `infinity-agents-edge`, custom domain
  `infinity.zhangyvjing.com`.
- New Worker version: `79755db3-c12d-4737-b601-aa99f11e3f93`.
- Deployment readback: deployment
  `a98fd1bd-ff46-4768-8cd8-af23dcc53fca`, version percentage `100`.
- Version readback: version number `151`, annotation
  `PAPER-10 Kimi K2.6 mainland endpoint correction`.
- Read-only `/health` readback: HTTP `200`, D1 and R2 configured, Paper
  Processor unconfigured.

The deployed non-secret provider variables are the reviewed mainland contract:
`MODEL_BASE_URL=https://api.moonshot.cn/v1` and `MODEL_ID=kimi-k2.6`.
The model credential was not read, changed, printed, or recorded.

## Acceptance stop

The existing authenticated Chrome tab was rediscovered by its current visible
URL/title (`https://infinity.zhangyvjing.com/` / `Infinity Agents`) on each
attempt. Three claim attempts were refused because the tab remained owned by
the source browser session. No DOM, cookie, storage, login data, or provider
request was read or sent. Therefore no `.cn` text-probe result is claimed,
and no Paper search, image, tool, D1, R2, or Processor acceptance was run.

PAPER-10 remains blocked on a controllable authenticated DOM. The corrected
Worker version is intentionally left at 100% so the next verified probe tests
the corrected endpoint; no StepFun rollback was performed.

## External-state boundary

This subphase changed only the authorized Edge Worker deployment. D1
migrations were not rerun; no D1 row, R2 object, WAF rule, Edge shared secret,
zhangbot token/release/service, Redis/Relay/Cloudflared state, or GitHub ref
was changed.

## 2026-08-29 text gate and WAF capability update

- Coordinator-provided real authenticated text-gate result against the
  corrected Worker version `79755db3-c12d-4737-b601-aa99f11e3f93`: exact
  prompt `请只回复：KIMI_MAINLAND_TEXT_PROBE_OK`; exact visible result
  `KIMI_MAINLAND_TEXT_PROBE_OK`.
- The prior international `.ai` 401 is superseded for this provider gate.
  Kimi K2.6 remains at 100%; StepFun was not used.
- The scoped WAF capability preflight then created and read back the exact
  fixed-endpoint BIC-only rule: entrypoint
  `65e15547ea3144feb70791fc155d1df0`, rule
  `5695e7eb000c4f49b77a616cff1411ae`, action `skip`, products `bic`, enabled
  and logged. The expression is recorded in `waf-current-rule.md`.
- This is not a PAPER-10 completion record. No Edge shared secret, zhangbot
  Processor token/release/service, R2 object, or D1 migration write has
  followed yet; D1 migrations remain applied and are not to be rerun.

## 2026-08-29 secret-handoff failure and rollback

- The Edge shared-secret write succeeded, but the matching SSH token handoff
  stopped before writing because the remote wrapper treated EOF on the
  newline-free stdin value as fatal. No Processor release or service started.
- Capability-first rollback removed the current WAF rule/entrypoint and read it
  back absent, deleted the Edge Secret, removed zhangbot staging, and removed
  the local temporary secret. This is a stopped attempt, not a deployment
  success; D1/R2 and existing Redis/Relay/Cloudflared were preserved.

## 2026-08-29 fresh preflight and stale-release isolation

- The current retry began from clean local HEAD
  `1fc350e7a80c5e330492e235641dea477eb88bee`. The real authenticated mainland
  Kimi text gate remains a precise PASS against Worker version
  `79755db3-c12d-4737-b601-aa99f11e3f93`; no provider secret value was read or
  recorded. The current secret-triggered 100% version was read back with the
  mainland `.cn` endpoint and `kimi-k2.6` model.
- Read-only Cloudflare/D1/R2/WAF checks passed with D1 migrations untouched,
  R2 unchanged at 15 objects/41.9 MB, no Paper shared secret, and no custom
  WAF entrypoint after rollback. Read-only zhangbot Python, service, listener,
  no-runner, fixed-egress, and source checks passed.
- An inert old release directory that was outside the active service path was
  moved, not deleted, to the exact 0700 quarantine path recorded in
  `baseline.txt`. The formal release root was read back empty. This was
  preparatory cleanup only; no Processor release, service, R2 object, D1 row,
  Edge secret, or Edge deployment was created in this retry.
- PAPER-10 remains incomplete. The next write is the fresh exact WAF rule and
  immediate readback; `deployment.txt` must not be interpreted as a PASS
  deployment record.

## 2026-08-29 exact WAF rule retry

- The first non-secret WAF payload was rejected HTTP `400`/error `20127`; the
  immediate entrypoint read remained `404`, with no partial state. The corrected
  request then created and read back entrypoint
  `6a212d8fb2444135a6b2511e7d8ad8d0` and rule
  `a7f6a28a87624da28d595a11eeb5d92b`.
- Readback validation passed for one enabled/logged zone custom rule, action
  `skip`, only BIC, exact fixed host/source/four method/path pairs, and no
  wildcard. This is not a full deployment or PAPER-10 PASS: no Edge shared
  secret, Processor token/release/service, R2 object, or full live acceptance
  has followed. The next action is the paired secret/token handoff.

## 2026-08-29 archive-precondition stop and rollback

- The release transfer stopped before `scp`: the local archive was named for
  prior HEAD `5e42e4e…`, while current HEAD was `5c4b7563…`. No release or
  Processor service was created.
- Capability-first rollback removed the WAF rule/entrypoint and verified 404,
  removed zhangbot token/staging, and deleted the Edge shared secret after a
  supported confirmation-input retry. The first `--yes` attempt was a CLI
  option error and was not treated as deletion.
- D1/R2, Kimi, and existing Redis/Relay/Cloudflared were preserved. This is a
  failed pre-install attempt, not a deployment or PAPER-10 PASS.

## 2026-08-29 Edge/zhangbot capability handoff

- The first local generator check stopped before external mutation at a
  newline-included 65-byte value. The corrected one-time 64-hex value was
  written to the Edge `PAPER_PROCESSOR_SHARED_SECRET` binding and delivered
  once to zhangbot via secure stdin. Edge name-only and zhangbot value-free
  metadata readbacks passed; the local temporary value was removed.
- Edge health is HTTP `200` with `paper_processor=configured`. No Processor
  release/service, R2 object, D1 migration, or Edge code deployment has
  followed. This remains a pre-install capability record, not a deployment or
  PAPER-10 PASS.

## 2026-08-29 current-HEAD artifact and preflight record

- No deployment or external write occurred in this subphase. The clean
  current HEAD `63cb12c2…` was packaged and validated locally, including the
  source/wheel archive hashes, pinned dependencies, required members, and
  no-AppleDouble checks.
- Read-only preflight confirmed the current 100% Worker remains on mainland
  Kimi (`https://api.moonshot.cn/v1`, `kimi-k2.6`), D1 `0017`–`0021` are
  applied and were not rerun, R2 remains 15 objects/41.9 MB, and health is
  200 with the Paper Processor unconfigured. The WAF entrypoint and Paper
  shared secret are absent.
- zhangbot read-only checks confirmed the exact `python3.10-venv` package,
  working venv/ensurepip/pip, unchanged existing services/listeners, and no
  Processor unit/release/process/token. This record authorizes only the next
  exact WAF create/readback, not a PAPER-10 completion claim.

## 2026-08-29 exact WAF capability record

- The exact fixed-endpoint BIC-only WAF rule was created and read back with
  HTTP `200` and semantic validation exit `0`: entrypoint `4eee604b…`, rule
  `08b7fc2e…`. It is the only new external change in this phase.
- No Edge shared secret, zhangbot token/release/service, D1 migration, R2
  object, or existing service change has followed. The next operation is the
  paired secret/token handoff, and any failure must remove this WAF rule first.

## 2026-08-29 Processor install hash-check failure and rollback

- The commit-named archive transfer and remote SHA/member checks passed. The
  install stopped before venv creation because its absolute-path aggregate did
  not match the relative-path manifest hash; lock/unit hashes matched.
- Capability-first rollback removed the WAF rule/entrypoint, Edge Paper secret,
  zhangbot token, and all new release/service/staging paths. No deployment,
  D1 migration, R2 object, or existing-service change occurred.
- This is a failed installer attempt, not a deployment or PAPER-10 PASS. The
  retry uses the manifest-relative hash command and a fresh preflight.

## 2026-08-29 latest Processor install stop

- The real mainland Kimi text gate passed with the exact visible marker
  `KIMI_MAINLAND_TEXT_PROBE_OK` against Worker version
  `79755db3-c12d-4737-b601-aa99f11e3f93`; this does not assert PAPER-10
  completion.
- The latest zhangbot install stopped at the no-AppleDouble check because
  `rg` is unavailable (exit `127`), before venv/release/unit/service creation.
  The WAF entrypoint and Edge Paper secret were removed/absent, and new
  zhangbot paths were removed/absent. No D1/R2/Edge code deployment occurred.
- This record is a failed attempt with completed rollback, not a deployment or
  PAPER-10 PASS. The next retry changes only the checker to `grep` and repeats
  a fresh read-only preflight.

## 2026-08-29 current preflight after mainland Kimi text gate

- Current local release HEAD is `9e5de68d…` on `cloudflare-deploy`; the
  commit-named source/wheel archives, manifest hashes, exact members, and
  no-AppleDouble checks passed. No source/migration changed.
- Current read-only production state is Worker version `f8470d1f…` (#161),
  mainland Kimi `.cn`/`kimi-k2.6`, fixed Processor bindings, absent Paper
  secret/WAF entrypoint, D1 no-pending/zero-write schema, R2 APAC/15 objects/
  41.9 MB, and health HTTP `200` with Processor unconfigured. zhangbot's
  package/venv/source egress and existing-service checks passed; Processor
  state is absent.
- Local Edge/Processor/frontend/E2E gates passed. This is a write-ready
  preflight only, not a deployment or PAPER-10 PASS. The next operation is the
  newly created exact WAF rule followed by paired capability handoff.

## 2026-08-29 retry preflight record

- No external write occurred in this preflight. The exact current-HEAD
  Processor archives passed manifest identity, pinned-wheel, required-member,
  and no-AppleDouble checks.
- Read-only Cloudflare and zhangbot checks passed: current 100% mainland Kimi,
  D1 migrations already applied/no write, R2 unchanged at 15 objects/41.9 MB,
  WAF/secret/Processor capability state absent, and existing host services
  preserved. This is not a deployment or PAPER-10 PASS.

## 2026-08-29 transfer command stop and rollback

- Both current-commit archives transferred to zhangbot staging, but the remote
  validation command was not run because SSH parsed the environment assignment
  as a hostname (`hostname contains invalid characters`, exit `255`). No
  release, venv, unit, service, or Edge deployment occurred.
- Recovery removed the new staging/token paths and confirmed Edge secret/WAF
  capability absent. Existing services and D1/R2 were preserved. This is not
  a deployment or PAPER-10 PASS.

## 2026-08-29 exact WAF capability create/readback

- The corrected zone custom WAF request created entrypoint `eec4070b…` and rule
  `026d95bb…` with HTTP `200`; immediate readback and semantic validation
  passed. The rule is enabled/logged, matches only the fixed zhangbot source,
  host, and four Processor method/path pairs, and skips only BIC.
- No Edge Paper secret, zhangbot token, Processor release/service, D1/R2,
  Edge-code, or existing-service write has occurred after this WAF write. The
  next authorized step is paired capability handoff; failure requires deleting
  the rule and entrypoint first.

## 2026-08-29 Processor transfer precondition stop

- Archive staging and SCP completed, but the remote validator stopped at SSH
  argument parsing (`hostname contains invalid characters`, exit `255`) before
  any install. The WAF rule/entrypoint and Edge Paper secret were then removed;
  the zhangbot token was removed with an exact absolute path after correcting a
  local `$HOME` quoting error. No release, service, D1, R2, or Edge-code
  deployment occurred.
- This is a failed transfer attempt with completed rollback, not a deployment
  or PAPER-10 PASS. The next attempt uses host-first SSH ordering and a fresh
  preflight.

## 2026-08-29 Processor archive-hash guard stop

- Host-first SSH and SCP reached the remote archive guard, but the archive SHA
  was compared with the source aggregate hash (`c440b41c…` versus `510715c4…`)
  due to a local variable mix-up. Wheel hash matched. No extraction, release,
  venv, unit, service, or Processor installation occurred.
- Capability-first cleanup deleted the new WAF rule/entrypoint and Edge Paper
  secret, and removed the zhangbot token/staging. No D1/R2/Edge-code or
  existing-service change occurred. This is not a deployment or PASS.

## 2026-08-29 immutable Processor archive transfer passed

- The host-first transfer and remote archive/member/AppleDouble checks passed
  with separate archive and source hash inputs. No release or systemd install
  has run yet. WAF entrypoint/rule `421a5184…`/`6dc66d74…` and Edge/zhangbot
  capability pairing are the only current external Paper changes.
- Next is commit-named release extraction and locked venv installation; any
  failure triggers WAF-first capability rollback. This is not a deployment or
  PAPER-10 PASS.

## 2026-08-29 retry preflight after transfer rollback

- Read-only local, Cloudflare, and zhangbot checks passed with current HEAD
  `9e5de68d…`, active Worker `52860ea1…` (#163), mainland Kimi bindings,
  applied D1/unchanged R2, absent Paper capability/WAF, fixed source egress,
  and preserved existing services. No external write occurred in this
  preflight.
- The exact corrected dpkg/venv/source checks passed; the next operation is a
  newly created/read-back WAF rule, followed by capability handoff and the
  corrected host-first archive validator. This is not a deployment or PASS.

## 2026-08-29 latest install rollback and retry readiness

- The latest transferred release passed immutable archive/source/lock/unit/
  manifest checks but stopped at locked offline pip because the wheel archive
  had not been extracted for `--find-links`; the redacted pip failure was no
  candidate for `pypdf==6.15.0` (exit `1`). No service or release became
  active.
- WAF-first rollback deleted the rule/entrypoint (HTTP `200`/`204`), removed
  the Edge Paper secret and zhangbot token, and cleaned staging/release/current/
  unit (all exit `0` with absence readbacks). D1/R2 and existing services were
  preserved; health remained HTTP `200`/unconfigured. PAPER-10 is not PASS.
- A fresh read-only preflight then passed for the current branch/artifacts,
  active mainland Kimi Worker `b70253c2…` (#169), applied D1/unchanged R2,
  absent WAF/secret/Processor state, zhangbot package/venv/source access, and
  existing service preservation. The next bounded install retries only the
  wheel-archive extraction before pip.

## 2026-08-29 capability handoff stop and rollback

- The fresh exact WAF rule readback passed (`2e8384f5…`/`d2151509…`) and the
  Edge Paper secret write/name read passed. The zhangbot one-line token
  handoff exited `1` during remote validation, so no Processor install ran.
- Rollback removed the WAF rule/entrypoint, Edge secret, and new zhangbot
  token/staging paths; all corresponding readbacks passed. Health remained
  HTTP `200`/unconfigured, D1/R2 and existing services were preserved. This
  is not a deployment or PAPER-10 PASS.
- The next bounded attempt uses direct stdin-to-temp-file handoff with
  portable validation, then repeats archive transfer and wheel extraction.

## 2026-08-29 transfer validator syntax stop and rollback

- Source and wheel transfer plus remote archive/wheel SHA checks passed, but
  the generated remote member validator exited `2` before extraction because
  of an unbalanced nested shell quote. WAF-first rollback removed the WAF
  rule/entrypoint, Edge secret, zhangbot token, and staging; absence reads
  passed. No Processor release/service, D1 migration, R2 write, or Edge code
  deployment occurred.
- The next bounded retry uses a literal remote validator command and retains
  the same immutable artifacts and capability-first rollback handles.

## 2026-08-29 archive wheel member-name stop and rollback

- The literal remote validator verified both archive SHA values and absent
  AppleDouble members, then stopped at wheel member matching because the tar
  stores the three wheel names with a leading `./`. No release or service
  install ran.
- WAF-first rollback removed the WAF rule/entrypoint, Edge secret, zhangbot
  token, and staging; readbacks passed and existing services remained active.
  No D1/R2/Edge-code write occurred. The next retry only normalizes the
  documented wheel tar prefix before the same install checks.

## 2026-08-29 latest archive-member rollback and fresh preflight

- The latest transfer stopped at the wheel member guard (exit `1`) after
  SHA/AppleDouble checks passed; the tar's legitimate wheel entries carry a
  leading `./`. No release or service install ran.
- WAF/Edge secret/zhangbot token/staging rollback and readbacks passed. A
  fresh preflight passed with active Worker `3dda1a0e…` (#175), mainland Kimi
  `.cn`/`kimi-k2.6`, applied D1/unchanged R2, absent capabilities, fixed
 zhangbot prerequisites, and preserved existing services. PAPER-10 is not
 PASS; next is only the prefix-normalized validator.

## 2026-08-29 temporary diagnostic wrapper invalidated

- The temporary diagnostic copied the archives but deleted its own input
  directory before validation, so its missing-file output is invalid. Cleanup
  completed and no production resource or service was changed.
- The next diagnostic only preserves the input directory and reports each
  stage; it does not create a production release or systemd service.

## 2026-08-29 marked install early stop and temporary diagnostic pass

- The production release wrapper stopped at an unreported pre-marker assertion
  (exit `1`) and was fully rolled back; no service became active.
- A corrected temporary diagnostic passed the same archive/source/manifest,
  wheel extraction, venv, and offline pip steps. It did not install systemd
  or change production resources. The next attempt adds stage markers to
  identify the production-only assertion.

## 2026-08-29 marked-install retry preflight

- The corrected temporary diagnostic passed all release prerequisites and
  was cleaned. Fresh read-only checks passed for active Worker
  `816e5144…` (#177), mainland Kimi, applied D1/unchanged R2, absent WAF/
  secret/Processor state, zhangbot prerequisites, and preserved services.
- Next is the exact WAF/capability sequence followed by the marked release
  install; PAPER-10 remains incomplete.

## 2026-08-29 production archive-root assertion stop and rollback

- The marked install passed input and release-parent setup, then exited
  `1` because the archive's root directory name was not moved into the
  required commit-only release path. No venv, unit, or service ran.
- Capability-first rollback removed WAF/Edge secret/zhangbot new paths and
  readbacks passed; health remained `200`/unconfigured and existing services
  were preserved. The next retry uses temporary extraction plus atomic root
  move, retaining the same immutable artifacts.

## 2026-08-29 marked install post-start readback stop and rollback

- The commit-named release was atomically assembled on zhangbot. Source,
  dependency-lock, service-unit, manifest identity, wheel-member, venv,
  ensurepip, offline pip, import, install-record, unit-content, symlink,
  daemon-reload, enable, and start steps all returned success. The wrapper
  stopped with `remote_install_rc=1` after `step=unit_start_returned`, before
  its service-readback pass marker; the individual active/enabled/MainPID/
  runner assertion values were not captured by that wrapper.
- The service start/stop pair is present in today's user journal. The older
  403/1010 `browser_signature_banned` application traceback is historical and
  was not emitted by today's start/stop readback window; it is retained as a
  prior access finding, not a new conclusion. A follow-up read found no
  Processor runner, token, unit, current symlink, or commit release. The
  staging parent remains empty.
- Capability-first rollback completed: WAF rule delete HTTP `200`, WAF
  entrypoint delete HTTP `204`, Edge Paper-secret deletion exit `0`, and
  host cleanup/absence readbacks exit `0`. Existing Redis/Relay/Cloudflared
  stayed active with only their established loopback listeners. No D1/R2/
  Edge-code deployment occurred.
- PAPER-10 remains incomplete. The next retry changes only post-start
  verification to emit redacted numeric return codes and runner count before
  enforcing the singleton assertion.

## 2026-08-29 capability handoff shape stop and rollback

- The fresh WAF rule/readback passed, and Edge
  `PAPER_PROCESSOR_SHARED_SECRET` put/name-readback returned `0`. The
  zhangbot stdin handoff stopped before installing the env file: the copied
  secret stream retained its final newline, so the temporary file had
  `line_count=2`; the 64-hex shape and mode `600` checks otherwise passed.
  No Processor release, unit, or service was started.
- Capability-first rollback deleted the WAF rule (HTTP `200`) and empty
  entrypoint (HTTP `204`), deleted the Edge secret (exit `0`), removed the
  token/temp paths on zhangbot, and read back Processor inactive/runner zero.
  Existing Redis/Relay/Cloudflared and their listeners were preserved; no D1
  migration, R2 object, or Edge deployment occurred.
- The next retry changes only stdin newline normalization before atomic env-file
  installation, then repeats the explicit service readback diagnostics.

## 2026-08-29 Edge/Processor release and browser-gated rollback

- The authorized release reached zhangbot successfully: immutable
  source/lock/unit hashes, offline wheels, venv/ensurepip, imports, unit
  hardening, user-systemd start, active/enabled/MainPID/runner readback, D1
  Processor session, and Edge readiness all passed. Edge code version
  `0c6a212f-b442-4872-b182-e32ccd0702e3` (#185) was deployed at 100% with
  the mainland Kimi/fixed Processor bindings.
- Required real browser acceptance could not start because the exact
  authenticated tab was already claimed by coordinating session
  `01a03d69-25c4-7ff0-94e7-9b3af0a8e627`; the current session had no
  controllable tab. This is a tooling/concurrency blocker, not a passing
  acceptance result.
- Capability-first rollback completed: WAF rule/entrypoint deletion
  `200`/`204` with final absence, Edge Paper secret deletion exit `0`,
  zhangbot stop/disable/daemon-reload and removal of only the new token/unit/
  current/release/staging exit `0`, and Edge rollback to
  `dc1e6477-4f5c-4e6a-b61d-331620719be2` exit `0`. Final health is
  `200/unconfigured`; no D1 migration, R2 write, or existing-service
  change occurred.

## 2026-08-30 Processor health deployment closure

- The fresh exact WAF rule was created and strictly read back before the host
  install: entrypoint `68dd39f74fee45d0a4c5b5120956eeb9`, rule
  `4a6264b8d93849ef9d0f20139268a08a`; enabled/logged action `skip`, BIC only,
  exact fixed source-IP/host/method/path expression.
- Source archive transfer, remote extraction, release hash checks, venv,
  ensurepip, offline `pip --require-hashes`, imports, install record, unit
  verification, daemon reload, enable, and start all exited `0`. The first
  wheel transfer was deliberately not used after remote tar exposed four
  AppleDouble members; the replacement USTAR archive used for installation
  has SHA `9ffe54a2e27cad1b0840479d9d2decf8ec092ac48028bff9073796f3385a3d66`
  and zero `._*` members.
- The release is
  `aa2306fe291fa31da73dd2d55223112cafafc580`, with source/lock/unit hashes
  matching `delivery.v1.json`, and `current` points to its immutable path.
  The service is enabled and `active/running` at MainPID `2052141`, with
  `ExecMainStatus=0` and `NRestarts=0`. Two stable samples, zero recent
  error-priority journal lines, and the runner's real request loop establish
  connect/poll health; no mock was used.
- Read-only zhangbot health returned HTTP `200` with
  `paper_processor=configured`. The Processor PID has no listening socket;
  Redis, Redis Relay, and Cloudflared remained active. The temporary upload
  directory was removed after installation.
- The Edge shared secret and zhangbot token were pre-provisioned by the
  coordinator and were not read or overwritten; no secret value is recorded.
  No D1 migration, R2 write, Kimi/provider change, Edge deployment, browser
  action, or existing-service change occurred in this subphase.
- Status: `PROCESSOR_HEALTH_PASS_BROWSER_ACCEPTANCE_PENDING`. PAPER-10 is not
  overall PASS. Rollback handles are the WAF IDs above, followed by stopping
  and removing only this new unit/current/release after read-only checks;
  preserve D1/R2 metadata and existing services.
