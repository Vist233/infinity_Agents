# PAPER-10 local release diff summary

The candidate change is limited to the Paper Workspace release surface:

- `backend/paper_processor/client.py`: fixed Edge control-plane validation and
  unique boot/process/nonce instance identity generation.
- `backend/paper_processor/delivery.v1.json`: versioned `v2` zhangbot runtime,
  artifact/hash, environment, secret, singleton, hardening, health, logging,
  and rollback contract.
- `backend/paper_processor/infinity-paper-processor.service`: hardened
  systemd-user unit with no listener and no embedded secret.
- `backend/requirements.paper-processor.zhangbot.txt`: exact dependency pins.
- `cloudflare-worker/wrangler.jsonc`: non-secret
  `PAPER_PROCESSOR_ID=paper-processor-zhangbot-v1` binding.
- `tests/test_paper_processor_runtime.py` and the delivery test: positive and
  negative runtime-contract coverage.
- `docs/CLOUDFLARE_PAPER_WORKSPACE_DESIGN.md`,
  `docs/CLOUDFLARE_PAPER_WORKSPACE_EXECUTION_PLAN.md`, and the Processor
  runbook: synchronized zhangbot-only deployment definition and operations.

The existing PubMed PMID abstract-only contract and
`PAPER_APPROVED_URL_DISABLED` entry-point rejection remain unchanged and are
covered by the full Edge suite. C7 Worker v2, D1/R2 schema SQL, Redis, Relay,
Cloudflared, frontend product code, and external systems were not altered.

No successful `deployment.txt` is present: the read-only Cloudflare and
zhangbot preflight passed and D1 migrations were applied, but Processor release
installation stopped at the missing zhangbot `python3.10-venv`/ensurepip
prerequisite. The temporary secret handoff and inactive release were rolled
back; service installation, Edge deployment, R2 processing, and authenticated
live acceptance did not occur.

The 2026-08-28 resume added no production code or configuration. It performed
the authorized read-only OS preflight, found the exact candidate
`python3.10-venv=3.10.12-1~22.04.17`, and stopped when
`sudo -n apt-get update` returned `sudo: a password is required`. No APT write,
alternative package, or sudo-policy change was attempted.

The final local repository check found no production diff and passed
`git diff --check`. A no-proxy live `ls-remote` could not resolve GitHub
(exit 128); the local tracking ref is not presented as a fresh remote
verification.

The subsequent no-secret evidence backup commit
`7c0b01b25205ed87aacacbc9f646f313b509fcc7` was pushed non-force (exit 0)
and independently verified by an elevated read-only `ls-remote` (exit 0)
with the exact matching `cloudflare-deploy` ref. No Cloudflare or zhangbot
state changed.

The owner-provided `python3.10-venv` installation was verified without a
write by SSH: package status, venv help/creation, ensurepip, and pip all
passed in a disposable directory. This only clears the host prerequisite;
no deployment or production resource change has occurred in the resumed
phase.

## 2026-08-28 systemd capability remediation

The first zhangbot start of the reviewed release failed with systemd
`218/CAPABILITIES` because the unprivileged user manager rejected
`PrivateDevices=yes`, `ProtectKernelTunables=yes`,
`ProtectKernelModules=yes`, and `ProtectControlGroups=yes`. A disposable
read-only probe matrix reproduced the failure for each unsupported control and
passed for the retained minimum hardening set. The checked-in unit now removes
only those four incompatible directives; `PrivateTmp`, `NoNewPrivileges`,
`ProtectSystem`, `ProtectHome`, `UMask`, identity/network restrictions,
`MemoryMax`, `TasksMax`, `LimitNOFILE`, controlled `ReadWritePaths`, and the
no-listener/single-instance contract remain. The delivery definition, design,
runbook, service hash, and regression coverage were updated together. The
failed deployment was rolled back before this remediation; D1/R2 metadata was
preserved and existing Redis/Relay/Cloudflared services were not changed.

## 2026-08-28 Edge access blocker

The corrected release installed successfully and passed unit/hash checks, but
the real zhangbot connect request was rejected at the fixed Edge hostname by
Cloudflare Error 1010 `browser_signature_banned` before the Worker handler.
The service, token, and Edge secret were then removed by the documented
capability-revocation-first rollback. No client/header bypass, arbitrary proxy,
alternate host, R2 write, Redis change, or migration rollback was attempted.
PAPER-10 remains blocked on a reviewed resolution for the exact Edge access
policy/client transport contract.

## 2026-08-28 Edge access contract amendment

The current uncommitted amendment is limited to the following Paper Workspace
release surfaces and evidence:

- `backend/paper_processor/delivery.v1.json`: fixed zhangbot source-IP,
  BIC-only edge-access scope, and exact method/path contract.
- `cloudflare-worker/src/paper-processor-access.ts`: application-level
  fail-closed source-IP and protocol-route gate (ignored by the repository's
  broad fixture rules and therefore must be force-added to the review commit).
- `cloudflare-worker/src/env.ts`, `src/index.ts`, and `src/paper-processor.ts`:
  non-secret binding, readiness requirement, and Worker defense-in-depth.
- `cloudflare-worker/wrangler.jsonc` and the three Processor/index/delivery
  regression test files: binding and positive/negative coverage.
- `docs/CLOUDFLARE_PAPER_WORKSPACE_DESIGN.md`,
  `docs/CLOUDFLARE_PAPER_WORKSPACE_EXECUTION_PLAN.md`, and
  `docs/PAPER_PROCESSOR_CLOUDFLARE_MANAGED_RUNBOOK.md`: auditable
  service-to-service contract and stop conditions.
- `evidence/IMPLEMENT-20260826-CF-PAPER/PAPER-10/edge-access-subphase.md`,
  this file, `tests-and-exit-codes.txt`, `secret-scan.txt`, and
  `checkpoint.md`: read-only findings and local gate evidence.

No Processor client User-Agent/proxy bypass was added. No D1/R2/Redis/Relay/
Cloudflared code or configuration was changed, no secret value was added, no
`deployment.txt` was created, and no external Cloudflare write occurred.

The reviewed amendment was committed as
`fb5ed60d22d9949d00381be41d0f5aea86e805c1` and pushed non-force to
`origin/cloudflare-deploy`. An independent read-only `ls-remote` returned the
same exact ref with exit code 0. This backup does not change the PAPER-10
status: the required Cloudflare rule was not created because the current
credential/plan cannot express and verify the exact minimum exception.

## 2026-08-28 fixed internal endpoint protocol amendment

This new local-only amendment is based on clean commit
`5ca83c60c6247e3271a639544c4233a791ef7860` and is limited to:

- `backend/paper_processor/client.py`: route all Processor traffic through the
  fixed `connect`, `poll`, `control`, and `object` endpoints; carry operation
  and leased identifiers in validated JSON/envelope data; never construct a
  dynamic attempt/object URL or accept a caller-selected R2 key.
- `cloudflare-worker/src/paper-processor-access.ts`: exact method/path route
  table with the source-IP gate; no dynamic route family remains eligible.
- `cloudflare-worker/src/paper-processor.ts`: strict control/upload operation
  and field allowlists, server-derived lease/resource authorization, and
  fixed route dispatch; retired dynamic paths fail closed before session use.
- `cloudflare-worker/test/paper-processor.test.ts`,
  `cloudflare-worker/test/paper-processor-delivery.test.ts`, and
  `tests/test_paper_processor_runtime.py`: positive and negative fixed-route,
  source/method/path, operation, ownership, lease, duplicate, cancellation,
  upload, and client-envelope coverage.
- `backend/paper_processor/delivery.v1.json`,
  `docs/CLOUDFLARE_PAPER_WORKSPACE_DESIGN.md`,
  `docs/CLOUDFLARE_PAPER_WORKSPACE_EXECUTION_PLAN.md`, and
  `docs/PAPER_PROCESSOR_CLOUDFLARE_MANAGED_RUNBOOK.md`: synchronized
  Free-plan BIC-only fixed-path rule, exact operation/object allowlists, and
  no-bypass/rollback instructions.
- `evidence/IMPLEMENT-20260826-CF-PAPER/PAPER-10/fixed-endpoint-subphase.md`,
  `baseline.txt`, `tests-and-exit-codes.txt`, `secret-scan.txt`, and
  `checkpoint.md`: this subphase's scope, local gates, security scan, and
  blocked checkpoint.

The exact exception candidate is finite and fixed:

```text
POST /api/paper-processor/connect
POST /api/paper-processor/poll
POST /api/paper-processor/control
PUT  /api/paper-processor/object
```

There is no compatibility handler for the former dynamic Processor paths.
The public browser/API protocol, D1/R2/Redis/Relay/Cloudflared contracts, and
all external systems remain unchanged. No Cloudflare rule, secret, token,
deployment, D1/R2 object, zhangbot service, or Redis/Relay/Cloudflared write
occurred in this amendment.

The fixed-endpoint implementation and its evidence were committed as review
commit `d87da003a741ac5ed5ef7015946776582c17ab13`. It was pushed non-force
from the expected `5ca83c60c6247e3271a639544c4233a791ef7860` remote tip, and a
read-only `git ls-remote --heads origin cloudflare-deploy` returned the exact
matching SHA with exit code 0. This GitHub backup does not change the
`BLOCKED_PROCESSOR_EDGE_ACCESS` / `WAITING_MINIMUM_WAF_CAPABILITY` status.

## 2026-08-28 minimum WAF capability preflight blocker

This local evidence-only update records a metadata check against the
owner-provided short-lived Cloudflare WAF token file. The file was present and
owned by the current user but had mode `0644`, not the required `0600`; the
check exited 4. Token contents were never read or persisted. Because the
precondition failed, no Cloudflare API request or external write was made, no
rule/read capability was inferred, and no deployment or live acceptance was
started. The new evidence file is
`evidence/IMPLEMENT-20260826-CF-PAPER/PAPER-10/waf-capability-preflight.md`.

## 2026-08-28 corrected-token WAF and immutable-artifact stop

This evidence-only update records the first successful minimum WAF capability
check after the token metadata was corrected to mode `0600`. The token was
used only through a secure stdin header pipe and no value was persisted. The
Rulesets read succeeded for the previously verified target zone; the broader
account/zone convenience reads returned API error `9109` under the scoped
token. The custom firewall entrypoint was absent, so there was no existing
custom rule order to replace.

The authorized exact zone entrypoint/rule was created and read back with
entrypoint ID `823074a217994347a3af06ee6e6f4a28` and rule ID
`8ee926f7fcf34736bbe565b2adbe0396`. The readback verified the fixed
`39.105.204.121` source, fixed host, exact four method/path pairs,
`action=skip`, `products=["bic"]`, logging enabled, and no wider action
parameters. No other Cloudflare rule was changed.

The next immutable release check then found the current Processor source
aggregate `510715c4a3e8605181219508d38bd8747b1fff28a7c676fb64d15fd1ed57d15e`
did not match the delivery-definition value
`ce76a75997ebff53c10a1baf2beb2631b66c8fb5a6740b469ba8bf04bf381813`.
The dependency lock and service unit still matched their pinned hashes. This
is recorded as `BLOCKED_PROCESSOR_ARTIFACT_HASH_MISMATCH`; no deployment
preflight was continued.

Capability-first rollback deleted the new rule (HTTP 200) and then the empty
new entrypoint (HTTP 204). The first wrapper exited 45 solely because it
treated successful HTTP 204 as failure. Independent reads confirmed the rule
was absent, the custom entrypoint returned HTTP 404, and the zone ruleset list
returned the original four non-custom rulesets. Net Cloudflare WAF state was
restored. No Secret, Processor, Edge, D1/R2 object, zhangbot, Redis, Relay, or
Cloudflared write occurred; `deployment.txt` remains absent.

## 2026-08-28 Processor delivery manifest hash reconciliation

Starting from clean commit
`c73e780b6c9dff342a6dfbbe8f3164ac2b4db520` on `cloudflare-deploy`, the
current source-hash inputs and algorithm were audited before editing. The
manifest's exact four-file input set is:

```text
backend/paper_processor/__init__.py
backend/paper_processor/client.py
backend/paper_processor/ingest.py
backend/paper_processor/runner.py
```

The exact declared command is:

```text
sha256sum backend/paper_processor/__init__.py backend/paper_processor/client.py backend/paper_processor/ingest.py backend/paper_processor/runner.py | sha256sum
```

Two independent runs agreed on aggregate
`510715c4a3e8605181219508d38bd8747b1fff28a7c676fb64d15fd1ed57d15e`.
`backend/paper_processor/delivery.v1.json` now pins that exact value. The
delivery test additionally computes the declared aggregate from the exact
input set and fails if source content changes without a manifest update.

Changed files are limited to the delivery manifest, its delivery regression
test, and PAPER-10 evidence. No runtime architecture, endpoint, secret
boundary, D1/R2 contract, or external system was changed. The WAF rule and
entrypoint remain absent from the prior rollback. `deployment.txt` remains
absent because PAPER-10 has not passed.

Review status: local gates and safety scans passed; the review commit and
non-force GitHub backup must be recorded/verified before the complete
immutable production preflight is repeated. D1 migrations `0017`–`0021` must
not be rerun.

## 2026-08-28 immutable preflight and WAF write

The review commit `793dec74a5ca6717e08e9083f673d648622b4095` was clean and
matched `origin/cloudflare-deploy` before this stage. The exact source,
dependency-lock, and systemd-unit identities matched the delivery manifest.
Read-only target checks matched account `3cfba3bb2ec69798aa4881b05d80810f`,
Worker `infinity-agents-edge`, zone `a6954af7cee9fcecb610d087bdce3e01`, D1
`infinity-agents-db` (`9ee9ec94-cb42-40b5-8372-681c7b57c105`), and R2
`infinity-agents-resources`. Production D1 markers/schema were present with
no pending migrations and no writes; migrations `0017`–`0021` were not rerun.

The exact four fixed endpoint BIC-only rule was then created after read-only
capability checks and read back exactly. New entrypoint
`f08c457a6ff54e52b17fda00ead62161` contains only rule
`7d0a2bb78b1b4634b7523a1c0902d37d`, with the fixed source IP/host/path set,
`skip`/`products=["bic"]`, enabled logging, and enabled state. This additive
WAF change is the only external write at this point. Edge secret, zhangbot
token/release/service, Edge deployment, R2 object, Redis, Relay, and
Cloudflared remain unchanged. A later failure must delete this rule and empty
entrypoint first, then revoke any subsequent capability material.

## 2026-08-28 external dependency-install stop and rollback

After the immutable preflight passed, this attempt created the exact
fixed-path BIC-only WAF rule, securely wrote the Edge shared secret, delivered
one matching token to zhangbot, and transferred the commit archive. The
archive's local and remote hash was
`41f0efd44628c33112d0ce51a2cdf7390a7213d0405188e1c4e1b2bd316375a3`; its
listing contained no macOS `._*` entries. The extracted source, dependency
lock, unit hash, and venv creation all passed.

The release stopped at pinned dependency installation. The remote pip command
exited `2` because the HTTPS read of `pypdf==6.15.0` from the package host
timed out. The release staging directory was cleaned by the controlled
failure trap, so no release/current/unit/service or Processor process existed.

Capability-first rollback was complete: WAF rule delete exited `0`/HTTP
`200`, rule readback showed zero rules, empty entrypoint delete exited
`0`/HTTP `204`, and final entrypoint readback was `404`/`10003`; Edge secret
deletion exited `0` and the name-only list showed it absent; zhangbot token
env and archive were removed; Redis/Relay/Cloudflared remained active and
unchanged; D1 migrations were not rerun. No R2 write was issued. The R2
bucket-info read temporarily returned `15/41.9 MB`, then `0/0 B`, then
`15/41.9 MB`; this read inconsistency is an unresolved retry blocker, not an
action attributed to this attempt. `deployment.txt` remains absent.

## 2026-08-28 dependency-closure retry

The retry added only the missing Python 3.10 transitive dependency pin
`typing_extensions==4.13.2`, synchronized
`dependency_lock_sha256` to
`51a30c24dc3af956d30e9891dbb3fb605c813b72f608f23507c7860c7a8c4775`, and
added a delivery regression assertion for that exact pin. No endpoint,
authorization, source allowlist, systemd hardening, D1/R2 schema, Redis, Relay,
Cloudflared, or public frontend behavior changed.

The corresponding external retry used a freshly rebuilt portable wheelhouse.
Its remote SHA-256 was
`241dce8114d95fe6a74ca86eb745dfe5687e8d58302e015d0d539bfcbf609b17`; the
source archive was
`5d694b88f413abe59c64c4276d49d4e924dad05bb272348807ecccf7006e174b`.
The corrected install reached offline pip and stopped because the prior lock
did not include pypdf's Python 3.10 `typing_extensions` dependency. The WAF
rule, Edge secret, zhangbot token, and transferred archives were rolled back
by exact identifiers/paths. Net external state after rollback is: D1
unchanged, R2 unwritten, Processor absent, and existing Redis/Relay/
Cloudflared active.

## 2026-08-28 secret handoff retry

This retry made no repository code change. It recreated the exact WAF
entrypoint/rule and wrote a temporary Edge shared secret, then stopped when
the SSH stdin wiring combined a secret pipe with a here-document and produced
a remote command-not-found diagnostic. The handoff was rejected as unsafe;
the one-time value was revoked and is not present in the repository or
evidence. Capability-first rollback removed the WAF rule/entrypoint, Edge
secret, and zhangbot token env. No release/current/unit/process, Edge code
deployment, D1/R2 write, or existing-service change remained.

## 2026-08-28 artifact-verifier retry

No repository code changed in this subattempt. The local release verifier
failed before transfer because its no-match branch attempted to raise `None`.
The exact WAF rule/entrypoint, Edge secret, and zhangbot token created before
that check were revoked by targeted rollback. A corrected minimal
Processor-only source archive and three-wheel archive passed local
AppleDouble validation, but have not yet been transferred or deployed.

## 2026-08-29 GitHub backup blocker

The artifact-verifier rollback evidence was committed locally as
`d67952b5bf560aebfbbf0671fab145fcb66718a6`. Proxy and direct GitHub access
failed (proxy connection refused; direct HTTPS timeout), so this commit is not
presented as remotely backed up. No Cloudflare/zhangbot state changed after
the rollback; the worktree is clean and `deployment.txt` is absent.

## 2026-08-29 release retry diff summary

- No product source, migration, frontend, Processor protocol, or runtime
  contract change was made in this retry. The release archive was built from
  backed-up commit `7f1944a6e056e469509e1eecf5f7df88b5358a12` and matched the
  checked-in source/lock/unit hashes.
- Only the PAPER-10 evidence records were amended after the controlled
  external attempt. The remote release path error and complete capability-
  first rollback are recorded without secret values or payloads.

## 2026-08-29 corrected smoke-check retry

- No product source or configuration changed. The release used the backed-up
  `a270b30ad4d144aad5431713d163dee8e7fcac4b` source and matching artifact
  hashes.
- The install reached venv/dependency completion, then stopped at a
  quote-unsafe import check. The release and newly issued capabilities were
  rolled back; only evidence is being amended.

## 2026-08-29 listener assertion retry

- No product source or runtime contract changed. The release reached
  systemd start, then the host-wide listener assertion rejected an existing
  cloudflared loopback port; no Processor listener was observed.
- The release and all newly created capabilities were rolled back. Only
  evidence is being amended.

## 2026-08-29 backed-up release retry

- No product source, migration, frontend, Processor protocol, or runtime
  contract changed. The current clean evidence baseline is
  `becde2db27aa7ef7e31bfb2ddbe4a0f4ce7be8cf`, and the non-force GitHub backup
  was confirmed by exact SSH `ls-remote`.
- The external retry reached systemd registration and exited `1` without a
  surfaced diagnostic. The release and all capabilities created for that
  retry were removed by exact target; only the PAPER-10 evidence records are
  amended.

## 2026-08-29 wheel archive metadata retry

- No product source, migration, frontend, Processor protocol, or runtime
  contract changed. The first wheel bundle carried macOS extended metadata
  into remote GNU tar validation; the release was stopped before install.
- The exact WAF/secret/token/staging changes from this retry were rolled back.
  A metadata-free replacement wheel archive was generated locally; only
  PAPER-10 evidence is being amended.

## 2026-08-29 systemd verification-order retry

- No product source, migration, frontend, Processor protocol, or runtime
  contract changed. The remote archive and locked dependency installation
  passed; the release stopped because systemd verification preceded the
  `current` symlink activation required by the unit's ExecStart path.
- The exact WAF/secret/token/release/staging changes were rolled back. Only
  PAPER-10 evidence is being amended.

## 2026-08-29 Kimi provider preflight rollback

- This subphase added only `provider-recheck.md` and the corresponding
  checkpoint/test evidence. No product source, migration, fixed-endpoint
  contract, WAF rule, Processor release, R2 object, or Redis/Relay/Cloudflared
  change was made.
- The real authenticated text probe failed with provider HTTP 401
  `Invalid Authentication`. The Kimi deployment was rolled back to
  `d287b02d-a94c-4caa-b473-70f2368f4999` at 100% and read back successfully.
- PAPER-10 remains blocked; no deployment artifact or PASS checkpoint was
  created. The provider credential value was never read or recorded.

## 2026-08-29 Kimi K2.6 browser-probe rollback

- This subphase added only `k26-browser-probe.md` and corresponding browser,
  checkpoint, test/exit-code, and diff evidence. No product source, migration,
  paper protocol, WAF, R2, Processor, or existing-service change was made.
- The authenticated browser tab remained owned by the source session, so the
  Kimi text provider result was not obtained. The Kimi candidate was rolled
  back to `d287b02d-a94c-4caa-b473-70f2368f4999` at 100% and read back.
- PAPER-10 remains blocked; no deployment artifact or PASS checkpoint was
  created. No credential value was read or recorded.

## 2026-08-29 live acceptance browser blocker

- No product source, migration, frontend, Processor protocol, or runtime
  contract changed. The reviewed Processor release and fixed-endpoint Edge
  deployment were exercised only until the browser acceptance entry point.
- The browser surfaces could not return an authenticated DOM, so all required
  live cases were stopped before execution. WAF/Secret/token/Processor state
  was rolled back and Edge was restored to the prior version; only PAPER-10
  evidence is being amended.

## 2026-08-29 Kimi K2.6 mainland endpoint correction

- The Worker provider configuration changed `MODEL_BASE_URL` from the
  international `https://api.moonshot.ai/v1` to the official mainland
  `https://api.moonshot.cn/v1`; `MODEL_ID` remains `kimi-k2.6`.
- Added regression assertions for the exact mainland text and image
  `chat/completions` URL, POST method, Bearer auth, model ID, streaming text,
  and base64 image message. Updated the existing delivery test to reflect the
  retained hash-pinned Processor lockfile.
- Existing uncommitted Processor manifest/lockfile changes were intentionally
  retained; no unrelated source, migration, R2, WAF, Redis/Relay/Cloudflared,
  or zhangbot state was changed.
- Local gates passed as recorded in `tests-and-exit-codes.txt`. No remote write
  has occurred yet; deployment and live acceptance remain outstanding.
## 2026-08-29 mainland endpoint deployment stop

- The local commit corrected the Kimi base URL to the official mainland
  endpoint and added text/image request-contract tests. The authorized Edge
  deployment created Worker version
  `79755db3-c12d-4737-b601-aa99f11e3f93` at 100%.
- The authenticated browser probe was stopped before any request because the
  exact existing tab was still owned by the source session. No browser or
  Paper data was transmitted, and no prior `.ai` 401 was attributed to `.cn`.
- No D1, R2, WAF, Edge Secret, zhangbot, Redis/Relay/Cloudflared, or GitHub
  change occurred in this subphase. PAPER-10 remains blocked.

## 2026-08-29 Kimi text gate and exact WAF rule

- Added redacted evidence for the real authenticated mainland Kimi text gate:
  exact prompt and exact visible `KIMI_MAINLAND_TEXT_PROBE_OK` result against
  Worker version `79755db3-c12d-4737-b601-aa99f11e3f93`.
- Added redacted evidence for the current zone WAF write/readback. The rule
  matches only `39.105.204.121`, host `infinity.zhangyvjing.com`, and the four
  fixed Processor method/path pairs; it skips only `bic` and keeps logging.
- No product source or migration changed in this evidence update. The WAF
  entrypoint/rule is the only current external Paper-release change; secret,
  token, Processor, R2, Redis/Relay/Cloudflared changes remain pending.

## 2026-08-29 fresh immutable preflight

- Added the current redacted preflight facts and exact active WAF readback to
  the PAPER-10 evidence. No production source or migration changed.
- The preflight confirms the corrected mainland Kimi text gate, matching
  Processor artifact hashes, exact Cloudflare/D1/R2 targets, no pending D1
  migration, and a clean zhangbot single-instance starting state.
- The actual remote branch ref was read only and is older than the local
  evidence HEAD; no GitHub push was performed in this continuation.

## 2026-08-29 secret handoff failure / rollback

- Added factual evidence for the failed newline-free SSH handoff and the
  subsequent capability-first removal of the exact WAF rule/entrypoint, Edge
  Secret, zhangbot staging, and local temporary secret.
- No product source, migration, R2 object, existing service, or Kimi provider
  configuration changed. The next retry changes only the wrapper's EOF
  handling and repeats the preflight before recreating capabilities.

## 2026-08-29 fresh immutable preflight after Kimi mainland text PASS

- Recorded a new read-only baseline from local HEAD
  `1fc350e7a80c5e330492e235641dea477eb88bee`. The manifest/source/lock/unit
  hashes match, and the tracked tree has no `._*` entries. The actual remote
  branch ref is `e551a5994cd228f19a2ae816c4529e4b04cf41a1`; no GitHub write was
  performed.
- The real authenticated Kimi mainland text result remains exact
  `KIMI_MAINLAND_TEXT_PROBE_OK` on Worker version
  `79755db3-c12d-4737-b601-aa99f11e3f93`; current read-only version metadata
  retains `https://api.moonshot.cn/v1` and `kimi-k2.6`. This does not assert
  Paper image, Processor, or full live acceptance.
- Cloudflare read-only target/D1/R2/health/WAF capability checks passed. D1
  was not rerun, R2 stayed at 15 objects/41.9 MB, the Paper secret name and
  custom WAF entrypoint are absent after rollback, and the four existing
  non-custom rulesets were not edited.
- zhangbot read-only runtime and four-provider fixed-egress checks passed.
  An inert stale old release was isolated by an exact reversible move into a
  0700 quarantine; no service or running process used it. The next permitted
  external write is a fresh exact WAF rule/readback, followed by the corrected
  secret/token handoff.

## 2026-08-29 exact WAF rule retry

- The first create request was rejected by Cloudflare's expression validator
  (HTTP `400`, error `20127`) and immediate readback confirmed no partial
  entrypoint. A corrected non-secret inline-set representation was submitted.
- The second create/readback passed: entrypoint
  `6a212d8fb2444135a6b2511e7d8ad8d0`, rule
  `a7f6a28a87624da28d595a11eeb5d92b`, enabled/logged, `skip` with only
  `products=["bic"]`, exact zhangbot source/host and fixed paths. No existing
  rule or security product was changed.
- This is a capability-only external update. No Edge secret/token, Processor
  release/service, R2 object, D1 migration, or Kimi provider configuration was
  changed after the WAF readback.

## 2026-08-29 Edge/zhangbot capability handoff

- The newline-terminated secret generator was rejected locally at 65 bytes;
  the corrected 64-hex temporary value was then used only via stdin. Edge
  secret write/name readback and the EOF-tolerant zhangbot one-line 0600 token
  handoff passed. No credential value was added to the tree or evidence.
- Edge health changed to `paper_processor=configured`; Processor installation,
  R2 object publication, D1 migration, Edge code deployment, and live Paper
  acceptance remain pending. Existing Redis/Relay/Cloudflared were unchanged.
- The current external capability set is the exact WAF rule plus its paired
  Edge/zhangbot secret material. The next step is immutable release transfer
  and installation with WAF-first rollback on any failure.

## 2026-08-29 archive HEAD mismatch and capability rollback

- Release transfer was blocked locally because current HEAD `5c4b7563…` had
  no matching archive; only the earlier `5e42e4e…` package existed. No stale
  package was reused.
- The active WAF rule/entrypoint, Edge shared secret, zhangbot token, and
  staging were rolled back. The initial secret-delete exit `1` was a CLI
  option error (`--yes` unsupported); the supported confirmation retry and
  name-only absence read passed. No product code, migration, R2 object, or
  existing service changed.
- Next attempt: package current HEAD, rerun preflight, then recreate exact WAF
  and capability material.
