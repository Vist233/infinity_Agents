# PAPER-10 local release tests

All commands below were run from the stated working directory. Exit codes are
recorded exactly; no failed assertion was skipped.

1. Delivery contract focused test
   Working directory: `cloudflare-worker`
   Command: `npm test -- --run test/paper-processor-delivery.test.ts`
   Result: 4 tests passed; exit code 0.

2. Processor runtime and ingestion focused tests
   Working directory: repository root
   Command: `eval "$(pyenv init - zsh)"; pyenv shell Agent; PYTHONPATH=. python -m pytest -q tests/test_paper_processor_runtime.py tests/test_paper_processor_ingestion.py`
   Result: 11 tests passed; exit code 0. pyenv emitted only its existing
   non-fatal rehash permission warning.

3. Mandatory Edge gate
   Working directory: `cloudflare-worker`
   Command: `npm run check && npm test`
   Result: TypeScript check exit code 0; 23 test files and 127 tests passed;
   overall exit code 0.

4. Frontend typecheck, lint and unit gate
   Working directory: `frontend`
   Command: `npm run typecheck && npm run lint && npm run test:unit`
   Result: typecheck exit code 0; lint exit code 0; 12 files and 50 unit tests
   passed; overall exit code 0. Existing React `act(...)` warnings were
   non-fatal and did not alter the result.

5. Frontend E2E gate, first environment attempt
   Working directory: `frontend`
   Command: `npm run test:e2e`
   Result: exit code 1 because the sandbox denied binding the test web server
   to `127.0.0.1:3000` (`EPERM`). No browser assertion failure occurred.

6. Frontend E2E gate, permitted local-server retry
   Working directory: `frontend`
   Command: `npm run test:e2e`
   Result: 13 tests passed; exit code 0. The Next.js proxy emitted expected
   local `localhost:8000` connection-refused diagnostics for an absent optional
   backend, while the E2E assertions all passed.

7. Diff and secret gates
   Commands: `git diff --check`; repository-scope credential-like pattern scan
   over the changed Paper/runtime/docs/config files.
   Result: diff check exit code 0; secret scan exit code 0 with no credential-like
   value, private key, credential-bearing URL, or raw payload match.

8. External operations in this local phase
   Result: this statement was superseded by the authorized PAPER-10 execution
   after the source backup and passing read-only preflight. The exact external
   results are below.

9. Production D1 migration apply
   Working directory: `cloudflare-worker`
   Command: `npx wrangler d1 migrations apply infinity-agents-db --remote`
   Result: interactive confirmation was supplied; `0017` through `0021` were
   each reported successful in order; exit code 0.

10. Production D1 migration readback
    Commands: `npx wrangler d1 migrations list infinity-agents-db --remote`;
    read-only `sqlite_master` query for all expected Paper tables/indexes;
    read-only `d1_migrations` query for migration IDs 17–21.
    Result: all commands exit code 0; no migrations pending; all expected
    tables/indexes present; migration IDs 17–21 recorded; readback metadata
    reports `changes=0`, `changed_db=false`, `rows_written=0`.

11. Edge shared-secret and zhangbot token creation
    Commands: `npx wrangler secret put PAPER_PROCESSOR_SHARED_SECRET --name
    infinity-agents-edge` with a temporary stdin-only random value; SSH stdin
    write to the exact zhangbot env file; read-only secret-name and env-shape
    checks.
    Result: secret put exit code 0; remote file write exit code 0; readback
    showed mode 600, one token key, 64 hex characters, and no printed value.

12. Processor release installation
    Command: reviewed source archive transfer followed by remote Python 3.10
    venv creation and pinned dependency installation.
    Result: exit code 1 at `python3 -m venv` because ensurepip is unavailable
    and the host lacks `python3.10-venv`. No dependency install, unit install,
    service start, Processor registration, Edge deploy, R2 write, or live
    acceptance followed.

13. Failure rollback
    Commands: targeted removal of the newly created commit-named release
    directory; targeted removal of the newly created zhangbot token file;
    `npx wrangler secret delete PAPER_PROCESSOR_SHARED_SECRET --name
    infinity-agents-edge`; read-only secret, zhangbot, migration, and health
    checks.
    Result: release cleanup exit code 0; token cleanup exit code 0; secret
    deletion exit code 0; all readbacks exit code 0. D1 migrations remain
    applied; Edge health is OK; Processor unit/env/release are absent; existing
    Redis/Relay/Cloudflared state is unchanged.

14. Resumed zhangbot OS preflight
    Command: read-only SSH check of `/etc/os-release`, architecture, Python,
    `dpkg-query`, `apt-cache policy python3.10-venv`, Processor unit/process,
    token/release paths, existing services, and listeners.
    Result: exit code 0; Ubuntu 22.04.5 LTS amd64, Python 3.10.12, exact
    candidate `python3.10-venv` version `3.10.12-1~22.04.17`, package absent,
    Processor state absent, and existing services unchanged.

15. Authorized APT precondition step
    Command: `ssh zhangbot 'sudo -n apt-get update'`
    Result: exit code 1; exact error `sudo: a password is required`. The
    authorized install command `sudo -n apt-get install --yes
    python3.10-venv` was not attempted. No package or system configuration
    changed.

16. Post-failure OS readback
    Command: read-only SSH check of package status/candidate, Processor
    env/release/unit, and existing service state.
    Result: exit code 0; package remains absent, candidate unchanged, no
    Processor state exists, and Redis/Relay/Cloudflared remain unchanged.

17. Final local repository and remote-ref verification
    Commands: `git status --short --branch`; `git rev-parse HEAD`;
    `git rev-parse origin/cloudflare-deploy`; `git diff --check`; and
    no-proxy `git ls-remote --heads origin cloudflare-deploy`.
    Results: local status/hash/diff checks exited 0 with clean
    `cloudflare-deploy` at `65b9a409f58197e81d47c0fc90e28002ac915987`.
    The no-proxy `ls-remote` exited 128 with exact error
    `Could not resolve host: github.com`; no remote value was inferred from
    that failed live check.

18. GitHub evidence backup and independent ref verification
    Command: non-force `git push origin HEAD:refs/heads/cloudflare-deploy`
    for commit `7c0b01b25205ed87aacacbc9f646f313b509fcc7`.
    Result: exit code 0; GitHub reported `65b9a40..7c0b01b HEAD ->
    cloudflare-deploy`.
    Read-only command: elevated no-proxy `git ls-remote --heads origin
    cloudflare-deploy`.
    Result: exit code 0; exact output was
    `7c0b01b25205ed87aacacbc9f646f313b509fcc7 refs/heads/cloudflare-deploy`.
    Local `git update-ref` used that exact 40-character SHA, and subsequent
    `git rev-parse origin/cloudflare-deploy` and `git status --short --branch`
    exited 0 with a clean worktree.

19. Resumed zhangbot runtime prerequisite verification
    Command: SSH read-only check of OS/Python/dpkg status, `python3 -m venv
    --help`, disposable `python3 -m venv`, temporary `ensurepip` and pip,
    Processor unit state, existing service state, and listeners.
    Result: exit code 0. Ubuntu 22.04.5 LTS x86_64; Python 3.10.12;
    `python3.10-venv` version `3.10.12-1~22.04.17`, status `install ok
    installed`; venv help/create, ensurepip, and pip all exit 0. Temporary
    venv was removed. Processor remains absent; Redis/Relay/Cloudflared remain
    active; listeners remain loopback-only 8090/6379.

20. Systemd hardening compatibility regression
    Commands: focused delivery test before remediation; then the checked-in
    service edit, service-hash update, delivery-definition test, and JSON/diff
    validation.
    Result: the first focused delivery assertion correctly failed exit code 1
    because `PrivateDevices=yes` was still present after the initial patch. It
    was removed without weakening the required controls; the corrected
    delivery test then passed 4/4 (exit code 0), Processor focused tests passed
    11/11 (exit code 0), JSON parsing and `git diff --check` both exited 0.
    The host probe evidence is preserved in the prior execution log: the old
    unit failed with systemd `218/CAPABILITIES`; the disposable compatible
    minimum-hardening probe exited 0.

21. Mandatory local gates after hardening remediation
    Commands and results:
    - `cd cloudflare-worker && npm run check && npm test`: TypeScript check
      exit 0; 23 files/127 tests passed; overall exit 0.
    - `cd frontend && npm run typecheck && npm run lint && npm run test:unit`:
      typecheck exit 0, lint exit 0, 12 files/50 tests passed; overall exit 0.
    - `cd frontend && npm run test:e2e`: 13 tests passed; exit 0. The run used
      the permitted local test-server capability; optional absent-backend
      proxy diagnostics were non-fatal and all assertions passed.
    - `git diff --check`: exit 0.

22. External failure rollback readback
    Commands: read-only systemd/journal diagnosis; targeted Edge secret delete;
    targeted Processor stop/disable/daemon-reload; targeted removal of the
    new token, unit, current symlink, and commit-named release; read-only
    service/listener/existing-service checks.
    Result: diagnosis exit 0 and recorded only the safe systemd error code;
    each rollback operation exited 0; post-rollback readback exited 0. No
    Processor unit, token, release, or current symlink remains. Existing
    Redis/Relay/Cloudflared services and loopback listeners are unchanged. The
    Edge deployment version remains recorded as `4ef4cea2-71ca-402e-aa4a-
    f5322417da2a`; the shared secret is absent after rollback. D1 migrations
    remain applied and no R2 object was written.

23. Post-remediation immutable-release preflight
    Commands: local branch/worktree/ancestor/hash checks; read-only Wrangler
    account, deployment/version, D1 migration/schema/marker, R2 bucket, and
    secret-name checks; public Edge health; read-only SSH package/venv/
    ensurepip/pip, Processor-state, existing-service, listener, and Docker
    checks.
    Result: all grouped preflight commands exited 0. Current local and exact
    remote ref are `118b63a87023d3e269924920a20412c8c0dfbcd2`; release commit
    `3558c1fec9035465407ca121fea94bd77e74d7bd` is an unchanged ancestor and
    all three delivery hashes match. D1 has no pending migrations and IDs
    17–21/tables are present with zero writes; R2 target exists; Edge health is
    configured except for the intentionally absent Paper shared secret.
    zhangbot reports exact `python3.10-venv` installed, venv/ensurepip/pip
    passing, no Processor state, active existing services, no Processor
    listener, and no Docker. No write occurred in this preflight.

24. Corrected release install and Edge deployment
    Commands: stdin-only secret handoff; verified archive transfer; zhangbot
    venv/offline wheel install; `pip check`; relative artifact/unit hashes;
    sanitized install record; systemd unit verification; `npx wrangler deploy`;
    read-only version and health readback.
    Results: secret put exit 0, token write/shape exit 0, archive transfer and
    readback exit 0, release install exit 0, install-record write exit 0,
    systemd verify exit 0, Edge deploy exit 0, version view exit 0, health exit
    0. One non-gating remote `rg` package-display subcommand returned 127
    (`rg: command not found`); the required package readback was rerun with
    available `grep` and exited 0. No secret value was printed.

25. Processor connect failure
    Commands: `systemctl --user daemon-reload`, `enable`, `start`; bounded
    state/readiness/listener readback; read-only `systemctl --user
    show/status` and `journalctl` diagnosis.
    Results: daemon-reload/enable/start each exit 0; bounded start verification
    exited 30 because the unit was `activating/auto-restart`, `MainPID=0`, with
    no Processor process. Journal diagnosis exited 0 and recorded the exact
    real Edge response: HTTP 403, Cloudflare Error 1010,
    `browser_signature_banned`, before the Worker handler. No manual retry was
    performed; only the unit's bounded automatic restart occurred during
    diagnosis.

26. Latest capability-revocation-first rollback
    Commands: targeted systemd stop/disable/daemon-reload and removal of the
    new Processor unit/current/release/token/archive; Edge secret delete;
    read-only Edge secret/health, D1 session/attempt aggregate, R2 bucket,
    zhangbot service/listener checks.
    Results: all rollback and readback commands exited 0. Edge health reports
    `paper_processor=unconfigured`; D1 reports zero sessions/attempts for the
    Processor with `rows_written=0` and `changed_db=false`; R2 target remains
    present with no Processor object write; zhangbot Processor state is absent;
    existing services/listeners are unchanged. `deployment.txt` was not
    created and no PASS checkpoint was written.

27. Blocker evidence GitHub backup
    Commands: `git push origin HEAD:refs/heads/cloudflare-deploy` followed by
    read-only `git ls-remote --heads origin cloudflare-deploy`; local tracking
    ref update used only the exact returned SHA.
    Result: non-force push exit code 0; read-only ref verification exit code 0;
    exact returned ref was
    `a35d110e608d4bfaa07c203c87fb3a8d5e03f657 refs/heads/cloudflare-deploy`.
    The worktree remained clean. This was a GitHub backup only; no Cloudflare
    write occurred in this evidence step.

28. Edge service-to-service access remediation subphase
    - Read-only local route/client inspection and contract comparison: exit
      code 0. The fixed connect/poll/input/lease/object endpoints, methods,
      session/lease headers, and source-IP gate were recorded without values.
    - Read-only zhangbot egress, service, process, Docker, and listener check:
      exit code 0. Three public providers agreed on `39.105.204.121`; no
      Processor service or listener was present; Redis/Relay/Cloudflared were
      unchanged.
    - Public invalid probes against the fixed Edge: shell exit code 0 for the
      requests; HTTP results were 405 for wrong-method connect, 401 for
      invalid Processor credentials, and 401 for an unknown Processor path.
      These probes did not create D1 state.
    - Read-only Cloudflare identity/zone/deployment checks: exit code 0.
      Ruleset/Firewall reads returned API error `10000 Authentication error`
      and security-setting reads returned `9109 Unauthorized to access
      requested resource`; no Cloudflare rule write was attempted.
    - Focused Edge regression tests: exit code 0, 3 files and 23 tests passed.
    - Mandatory Edge `npm run check && npm test`: exit code 0; typecheck and
      23 files/128 tests passed.
    - Processor focused pytest under `pyenv shell Agent`: exit code 0; 11
      tests passed. The pyenv rehash warning was non-fatal and unrelated to
      the test result.
    - Frontend `npm run typecheck && npm run lint && npm run test:unit`:
      exit code 0; typecheck/lint passed and 12 files/50 tests passed.
    - Frontend E2E: first sandbox-only attempt exit code 1 because the local
      test server could not bind `127.0.0.1:3000`; the permitted local
      test-server rerun exit code 0 with 13 tests passed. The optional absent
      backend proxy diagnostics were non-fatal.
    - No Cloudflare write, D1/R2 object write, Edge deployment, zhangbot
      release/service/token write, Redis change, or Cloudflared change was
      made in this subphase.

29. Review commit and GitHub backup
    - Local commit `fb5ed60d22d9949d00381be41d0f5aea86e805c1`: exit code 0.
      The commit contains 17 reviewed files and no secret values.
    - Non-force `git push origin HEAD:refs/heads/cloudflare-deploy`: exit
      code 0; target advanced from `e72ad26b` to `fb5ed60`.
    - Read-only `git ls-remote --heads origin cloudflare-deploy`: exit code 0;
      exact ref was
      `fb5ed60d22d9949d00381be41d0f5aea86e805c1 refs/heads/cloudflare-deploy`.
    - Local `git status --short --branch` after the commit: exit code 0 and
      clean `cloudflare-deploy` before this evidence-only amendment.

30. Fixed internal endpoint focused regression gate
    - Working directory: `cloudflare-worker`.
    - Command: `npx vitest run test/paper-processor.test.ts test/index.test.ts
      test/paper-processor-delivery.test.ts`.
    - Earlier attempts in this subphase exited 1 while the synchronized
      runbook assertion still expected the former route contract; those
      failures were corrected in the scoped implementation/docs change and
      were not treated as acceptance.
    - Final result: 3 files and 23 tests passed; exit code 0. Coverage
      includes fixed routes, wrong method/source/path, operation and field
      allowlists, cross-resource/attempt, stale lease, duplicate/finalize,
      cancellation, upload, and credential negatives.

31. Final Processor regression gate
    - Working directory: repository root.
    - Command: `eval "$(pyenv init - zsh)"; pyenv shell Agent; PYTHONPATH=.
      python -m pytest -q tests/test_paper_processor_runtime.py
      tests/test_paper_processor_ingestion.py`.
    - Result: 12 tests passed; exit code 0. The existing pyenv rehash
      permission warning was non-fatal. The runtime test asserts that the
      client emits only the four fixed paths, no query/dynamic attempt path,
      and the expected control/upload envelopes.

32. Final mandatory Edge gate
    - Working directory: `cloudflare-worker`.
    - Command: `npm run check && npm test`.
    - Result: TypeScript check exit code 0; 23 test files and 128 tests passed;
      overall exit code 0.

33. Final frontend typecheck, lint and unit gate
    - Working directory: `frontend`.
    - Command: `npm run typecheck && npm run lint && npm run test:unit`.
    - Result: typecheck exit code 0; lint exit code 0; 12 test files and 50
      unit tests passed; overall exit code 0. Existing React `act(...)`
      warnings were non-fatal.

34. Final frontend E2E gate
    - Working directory: `frontend`.
    - Command: `npm run test:e2e` with the permitted local test-server
      capability because the ordinary sandbox denied `127.0.0.1:3000`.
    - Result: 13 tests passed; exit code 0. The optional absent-backend proxy
      emitted connection-refused diagnostics, but all browser assertions
      passed. The earlier sandbox-only attempt is retained above as exit 1
      with `EPERM` and was not counted as a test failure.

35. Final diff and secret gates
    - Commands: `git diff --check`; changed-scope `rg -l -i --no-messages`
      scan for private keys, credential prefixes, long bearer values,
      credential-bearing URLs, and non-placeholder Processor secret
      assignments. The scan explicitly excluded this detector-pattern record
      itself.
    - Results: `git diff --check` exit code 0; raw no-match `rg` exit code 1,
      normalized secret-scan gate exit code 0. No secret value, parent
      credential, or raw PDF/image/text/tool payload was found.

36. External-operation boundary for this subphase
    - Result: no Cloudflare rule/secret/token/deployment, D1 migration or R2
      object write, zhangbot release/service/token write, Redis/Relay/
      Cloudflared change, or live acceptance was performed. The only planned
      external action after local gates is the authorized GitHub backup of the
      review commit; PAPER-10 remains blocked pending minimum WAF capability.

37. Fixed-endpoint review commit and GitHub backup
    - Pre-push read-only command: `git branch --show-current`,
      `git status --short --branch`, `git rev-parse HEAD`,
      `git rev-parse origin/cloudflare-deploy`, and
      `git ls-remote --heads origin cloudflare-deploy`; exit code 0. The
      branch was `cloudflare-deploy`, the worktree was clean, HEAD was
      `d87da003a741ac5ed5ef7015946776582c17ab13`, and the remote ref before
      push was `5ca83c60c6247e3271a639544c4233a791ef7860`.
    - Review commit: `git commit -m "fix: move paper processor to fixed
      endpoints"`; exit code 0, created
      `d87da003a741ac5ed5ef7015946776582c17ab13` with the 16-file local
      implementation/test/docs/evidence change.
    - Non-force push: `git push origin HEAD:refs/heads/cloudflare-deploy`;
      exit code 0. GitHub advanced `5ca83c6` to `d87da00` on
      `cloudflare-deploy`.
    - Read-only post-push verification: `git ls-remote --heads origin
      cloudflare-deploy`; exit code 0, exact output was
      `d87da003a741ac5ed5ef7015946776582c17ab13 refs/heads/cloudflare-deploy`.
      `git rev-parse HEAD` matched that SHA and the final worktree remained
      clean. This was the only external operation in this subphase: a GitHub
      source backup, not a Cloudflare or zhangbot change.

38. Minimum WAF capability token preflight
    - Working directory: repository root.
    - Read-only command checked the expected short-lived token file's
      existence, owner, and mode without opening or printing its contents.
    - Result: the file exists and is owned by the current user, but its mode is
      `0644` rather than the required `0600`; the check exited with code 4.
    - Per the stop rule, no token content was read, no Cloudflare API request
      was attempted, and no WAF rule, secret, deployment, D1/R2, zhangbot,
      Redis, Relay, or Cloudflared write occurred. WAF capability and target
      preflight therefore remain unverified.

39. Corrected-token WAF read-only preflight
    - Working directory: repository root.
    - Token metadata check (existence, owner, mode only): exit code 0; mode
      was `0600`. Token contents were not printed, persisted, hashed, or
      included in evidence.
    - Secure token verification request: curl exit code 0, HTTP 200, API
      result `active`.
    - Account and zone convenience reads: curl exit code 0, HTTP 403, API
      error 9109. Exact target-zone Rulesets list: curl exit code 0, HTTP 200,
      four existing non-custom rulesets. Custom-firewall entrypoint read:
      curl exit code 0, HTTP 404, API error 10003.

40. Exact fixed-endpoint WAF rule create/readback
    - Initial entrypoint GET: curl exit code 0, HTTP 404.
    - Additive zone entrypoint POST: curl exit code 0, HTTP 200. IDs were
      recorded as `823074a217994347a3af06ee6e6f4a28` and
      `8ee926f7fcf34736bbe565b2adbe0396`; no secret value was recorded.
    - Immediate entrypoint GET: curl exit code 0, HTTP 200; exact expression,
      method/path set, BIC-only action parameters, enabled logging, and
      enabled state validated by a dedicated readback assertion (exit code 0).

41. Immutable artifact identity preflight
    - The exact source aggregate hash command exited 0 and returned
      `510715c4a3e8605181219508d38bd8747b1fff28a7c676fb64d15fd1ed57d15e`,
      which does not equal the delivery-definition value
      `ce76a75997ebff53c10a1baf2beb2631b66c8fb5a6740b469ba8bf04bf381813`.
      Dependency-lock and systemd-unit hash checks exited 0 and matched.
    - Stop status: `BLOCKED_PROCESSOR_ARTIFACT_HASH_MISMATCH`. No D1/R2,
      Secret, Processor, Edge, zhangbot, Redis, Relay, or Cloudflared write
      was attempted after this mismatch. D1 migrations 0017–0021 were not
      rerun. Full post-deployment/live tests were not run because the
      immutable release gate failed before deployment.

42. Capability-first WAF rollback
    - Readback before rollback: curl exit code 0, HTTP 200; exactly one
      marker rule in the newly created entrypoint.
    - Rule DELETE: curl exit code 0, HTTP 200. Post-rule-delete GET: curl
      exit code 0, HTTP 200; zero rules and marker absent.
    - Entrypoint DELETE: curl exit code 0, HTTP 204. The wrapper returned
      exit code 45 only for an incorrect local `200`-only assertion; the API
      deletion was successful. Independent final entrypoint GET: curl exit
      code 0, HTTP 404; post-rollback ruleset list: curl exit code 0, HTTP
      200 with the original four rulesets and no custom entrypoint.
    - Net Cloudflare WAF state was restored. No Secret/token, service,
      release, Edge deployment, D1/R2 object, zhangbot, Redis, Relay, or
      Cloudflared rollback was needed.

43. Processor source manifest drift remediation
    - Clean baseline readback: branch `cloudflare-deploy`, worktree clean,
      local HEAD and `origin/cloudflare-deploy` both
      `c73e780b6c9dff342a6dfbbe8f3164ac2b4db520`; each command exited 0.
    - Exact hash input/algorithm audit: two runs of the declared
      `sha256sum <four fixed files> | sha256sum` algorithm exited 0 and
      produced the same aggregate
      `510715c4a3e8605181219508d38bd8747b1fff28a7c676fb64d15fd1ed57d15e`.
    - Focused delivery test before manifest update: exit code 1 as an
      intentional red test (4/5 passed; the new synchronization assertion
      detected the old value). After the manifest update: exit code 0, 5/5.

44. Full local PAPER-10 gates after manifest reconciliation
    - Edge `npm run check && npm test`: TypeScript exit 0; 23 files and 129
      tests passed; overall exit 0.
    - Processor `eval "$(pyenv init - zsh)"; pyenv shell Agent; PYTHONPATH=.
      python -m pytest -q tests/test_paper_processor_runtime.py
      tests/test_paper_processor_ingestion.py`: exit 0; 12 tests passed.
      The pyenv shim rehash warning was non-fatal.
    - Frontend `npm run typecheck && npm run lint && npm run test:unit`:
      typecheck, lint, and overall exits 0; 50 unit tests passed.
    - Frontend first `npm run test:e2e`: exit 1 because the sandbox denied
      binding `127.0.0.1:3000` with `EPERM`. The permitted local-server rerun:
      exit 0; 13 E2E tests passed. Backend proxy refusals were optional and
      non-fatal to the browser assertions.
    - `git diff --check`: exit 0. Changed-source raw no-match secret scan:
      exit 1; normalized gate exit 0.

45. Manifest-remediation review backup
    - The review diff contained only the delivery source hash and the exact
      source-input/algorithm regression test before evidence files were
      included; no secret or runtime architecture change was made.
    - The follow-up commit and non-force GitHub push/`ls-remote` verification
      are recorded after the evidence update. Cloudflare and zhangbot remain
      untouched pending the fresh immutable preflight.

46. Manifest-reconciliation pre-commit safety gate
    - `git diff --check`: exit code 0.
    - Changed-scope secret scan: raw no-match `rg` exit code 1; normalized
      secret-scan gate exit code 0. No secret value or raw paper payload was
      present in the current local change.

47. Immutable preflight after review backup
    - Local artifact identity command: exit 0; source aggregate
      `510715c4a3e8605181219508d38bd8747b1fff28a7c676fb64d15fd1ed57d15e`;
      dependency lock and service unit hash checks: exit 0 and matched.
      Node identity assertion: exit 0 (`artifact identity: PASS`).
    - `git status`/branch/HEAD/remote verification: exit 0; clean
      `cloudflare-deploy`; local and remote review commit
      `793dec74a5ca6717e08e9083f673d648622b4095` matched.
    - `npx wrangler whoami`: exit 0; account ID matched the checked-in target.
      Deployment inventory JSON, secret-name list, R2 bucket list/info, and
      public health check exited 0. Health was HTTP 200 with sanitized
      `status=ok`, `service=infinity-agents-edge`.
    - Remote D1 migrations list: exit 0, no pending migrations. Read-only
      marker/schema queries: exit 0, all five markers and all required Paper
      tables/indexes present, with zero writes. Migrations 0017–0021 were not
      rerun.
    - WAF token metadata and token verification: exit 0. Target rulesets read:
      exit 0/HTTP 200; custom entrypoint read: exit 0/HTTP 404 before write.
      The exact rule POST: exit 0/HTTP 200; immediate exact readback assertion:
      exit 0 (`validation=PASS`).
    - zhangbot SSH runtime preflight: exit 0; Ubuntu 22.04, installed
      `python3.10-venv`, venv/ensurepip/pip all exit 0, existing Redis/Relay/
      Cloudflared active, Processor unit/process/release/token absent, no
      Docker, no Processor listener, four independent egress providers
      matched `39.105.204.121`, and arXiv/PMC checks returned HTTP 200.
    - No D1/R2 object, Edge secret, Processor token, Processor release/service,
      or Edge deployment write occurred before the WAF rule. `deployment.txt`
      remains absent.

48. Processor archive transfer and failed dependency installation
    - The first local archive lookup command exited `1` after traversing
      unrelated macOS temporary paths; it did not contact zhangbot. The
      corrected explicit archive transfer exited `0`; local and remote
      archive SHA-256 both equaled
      `41f0efd44628c33112d0ce51a2cdf7390a7213d0405188e1c4e1b2bd316375a3`,
      and the remote archive contained no `._*` entries.
    - Remote extraction/artifact checks and `python3 -m venv` exited `0`.
      Pinned pip installation exited `2` due to an HTTPS read timeout while
      fetching `pypdf==6.15.0`; no release/current/unit/service was created.
      Staging cleanup completed through the controlled trap.

49. PAPER-10 capability-first rollback and post-state checks
    - WAF rule delete: curl exit `0`/HTTP `200`; readback exit `0`/HTTP `200`
      with zero rules. Entrypoint delete: exit `0`/HTTP `204`; final readback
      exit `0`/HTTP `404`/`10003`; ruleset inventory exit `0`/HTTP `200` with
      the original four non-custom rulesets.
    - Edge secret deletion: Wrangler exit `0`; name-only secret list exit
      `0` and no `PAPER_PROCESSOR_SHARED_SECRET`. zhangbot token env and
      archive removal exited `0`; Processor remained not-found/inactive/dead.
    - Final zhangbot units/listeners check exited `0`; Redis, Relay, and
      Cloudflared retained the same active state/main PIDs and listener set.
      D1 migration read exited `0` with no pending migrations. Edge health
      exited `0`/HTTP `200`. R2 metadata read was inconsistent: `15/41.9 MB`,
      then `0/0 B`, then `15/41.9 MB`; no R2 write/delete command was issued.

50. Retry immutable preflight and archive diagnosis
    - The initial Wrangler read-only command through the configured proxy
      exited `1` on the unavailable `127.0.0.1:7897` proxy before D1 state
      was read. With proxy variables removed, the same secret-name, D1
      migration, R2 metadata, and WAF read-back command exited `0`: the
      secret name was present, D1 returned `No migrations to apply!`, R2
      returned `15` objects/`41.9 MB`, and the exact WAF rule read back as
      one enabled logged `skip` rule with `products=["bic"]`.
    - Read-only zhangbot runtime preflight with the corrected dpkg format
      exited `0`: Ubuntu 22.04, `python3.10-venv` installed/candidate
      `3.10.12-1~22.04.17`, venv/ensurepip/pip exit `0`, existing Redis/
      Relay/Cloudflared active, Processor inactive/no release/current, no
      Processor listener, and only the one mode-600 token key present.
    - The first remote archive validation exited `43`: the source archive
      was valid, while the older wheelhouse contained `./._pymupdf...` and
      `./._pypdf...`. A portable GNU-format wheelhouse was then generated;
      local validation exited `0`, transfer exited `0`, and remote hash/
      member/AppleDouble validation exited `0`.

51. Dependency-closure release retry and rollback
    - The first release wrapper stopped before its candidate check because
      it used the wrong extracted root name; no pass marker or release was
      produced. Its wrapper did not capture a numeric exit code, so it is not
      treated as an acceptance result.
    - The corrected release install exited `1` after venv creation and
      archive validation. Offline pip reported that `typing_extensions>=4.0`
      required by `pypdf==6.15.0` was unavailable; the staging trap removed
      all temporary release data. No release/current/unit/service/process or
      R2 object was created.
    - WAF rule deletion exited `0`/HTTP `200`; empty entrypoint deletion
      exited `0`/HTTP `204`; final WAF read returned the expected not-found
      error. Edge secret deletion exited `0` and the name-only list confirmed
      absence. zhangbot cleanup exited `0`; token env, release/current/unit,
      and retry archives were absent, while Redis/Relay/Cloudflared stayed
      active.

52. Local dependency closure and gates
    - pypdf wheel metadata inspection exited `0` and confirmed the conditional
      `typing_extensions >= 4.0` requirement. Direct PyPI download of
      `typing_extensions==4.13.2` exited `0` with wheel SHA-256
      `a439e7c04b49fec3e5d3e2beaa21755cadbbdc391694e28ccdd36ca4a1408f8c`.
    - Focused delivery test exited `0` (`5` tests). Edge
      `npm run check && npm test` exited `0` (`23` files/`129` tests).
      Processor pytest exited `0` (`12` passed). Frontend typecheck/lint/unit
      exited `0` (`50` unit tests). The first frontend E2E attempt exited
      `1` only because the sandbox denied binding `127.0.0.1:3000`; the
      permitted local-server retry exited `0` (`13` passed).
    - `git diff --check` exited `0`; the working-tree no-match secret scan
      had raw exit `1` and normalized gate exit `0`.

53. Review commit and GitHub backup
    - `git commit -m 'fix: close paper processor Python 3.10 dependency
      closure'` exited `0` and created
      `96dce21a302ebe258f1ae6de343ae8b148bde76e`.
    - Direct non-force `git push origin HEAD:refs/heads/cloudflare-deploy`
      exited `0`. Direct read-only `git ls-remote --heads origin
      cloudflare-deploy` exited `0` and exactly matched the same SHA/ref.
      Final `git status --short --branch` exited `0` with a clean
      `cloudflare-deploy` worktree.

54. Secret handoff retry and capability-first rollback
    - The fresh read-only preflight exited `0`; exact WAF creation exited
      `0`/HTTP `200`, and immediate readback exited `0`/HTTP `200` with
      entrypoint `1aa5798c03814e48a95466764dc9d9c6`, rule
      `ac11b4092cf64d3b8a912c455f6f75bd`, and validation `PASS`.
    - Edge secret write exited `0`. The combined SSH handoff wrapper returned
      `0` but emitted a remote `command not found` diagnostic because a
      here-document competed with the secret stdin pipe. The reported env
      shape was therefore not accepted as a valid handoff; no Processor
      process or release was started.
    - Exact WAF rule deletion exited `0`/HTTP `200`; empty entrypoint deletion
      exited `0`/HTTP `204`; final readback returned the expected not-found
      error. Edge secret deletion and name-only absence check exited `0`.
      zhangbot token removal and final Processor/release/current checks exited
      `0`; Redis/Relay/Cloudflared remained active. No D1 migration, R2 write,
      Edge deployment, or Redis/Relay/Cloudflared write occurred.
    - The one-time secret was transiently exposed by the malformed shell
      diagnostic; it was not written to the repository, evidence, service
      logs, or a persistent release, and was invalidated before continuation.

55. Artifact packaging verifier retry and rollback
    - The third retry read-only preflight exited `0` after correcting the
      repository-root path. Exact WAF create/readback exited `0`/HTTP `200`;
      the corrected single-stdin secret/token handoff exited `0`, with remote
      env mode `600`, one key, and one line.
    - Minimal source/wheel archive generation completed, but its first
      validation wrapper exited `1` because the no-AppleDouble branch used an
      invalid `raise None` expression. No archive was transferred and no
      release/service/Edge deployment/R2 operation followed.
    - Capability-first WAF rollback exited `0`/HTTP `200` for the rule and
      `0`/HTTP `204` for the entrypoint; final readback returned not-found.
      Edge secret deletion/name-only absence exited `0`; zhangbot token
      removal and final service checks exited `0`; existing services stayed
      active and D1/R2 were unchanged.
    - Corrected minimal archive generation/validation exited `0`: source
      archive SHA-256 `b15487530515787a69b4a2592e796d09c6ca841837236371c472d263141edf8d`,
      wheel archive SHA-256
      `0556776a06168ac46933365d7e778d532a4c251c62015e4b46cea69e647aefce`,
      both with no `._*` members.

56. GitHub backup retry and blocker
    - Corrected minimal source/wheel archive generation and AppleDouble
      validation exited `0`; no transfer or production write followed.
    - `git commit -m 'docs: record PAPER-10 artifact verifier rollback'`
      exited `0` and created local commit
      `d67952b5bf560aebfbbf0671fab145fcb66718a6`.
    - Proxied `curl -I https://github.com` exited `7` on
      `127.0.0.1:7897`; direct `curl --noproxy '*' -I https://github.com`
      exited `28` after connection timeout. Bounded direct `git ls-remote`
      and non-force `git push` each exited `142` after the 15-second alarm,
      with no remote update. The last confirmed remote ref is
      `3039acc5cc3b169ecb5d0c4b2090e980b35ee95d`.
    - Final local `git status --short --branch` exited `0` with a clean
      `cloudflare-deploy` worktree. PAPER-10 remains blocked on GitHub backup.

57. Bounded GitHub retry after local evidence commit
    - `git ls-remote --heads origin refs/heads/cloudflare-deploy` under the
      configured network exited `128` with a TLS `SSL_ERROR_SYSCALL`; it did
      not return a ref.
    - The same read-only `ls-remote` with all proxy variables removed exited
      `142` after the 15-second alarm. Repeating it with direct HTTP/1.1 also
      exited `142`; no remote state was changed.
    - Non-force `git push origin HEAD:refs/heads/cloudflare-deploy` under the
      configured network exited `128` with the same TLS error. The direct
      retry and direct HTTP/1.1 retry each exited `142` after the 20-second
      alarm; no remote state was changed.
    - No Cloudflare, zhangbot, D1, R2, Redis, Relay, or Cloudflared write was
      performed in this retry. The last confirmed remote ref remains
      `3039acc5cc3b169ecb5d0c4b2090e980b35ee95d`.

58. Backed-up immutable preflight, exact WAF, secret/token, and release retry
    - Local source/lock/unit hash verification exited `0`; no-proxy Wrangler
      account, Worker version, D1 list/migration/schema, R2 bucket info,
      Secret-name, and health reads exited `0`. D1 migrations were not run.
    - WAF token metadata/verify and Rulesets/entrypoint reads exited `0`, with
      token mode `0600`, active token, custom entrypoint HTTP `404`, and no
      existing custom rule. Exact rule create/readback exited HTTP `200` and
      the exactness gate exited `0`.
    - Edge Secret initial write/name readback exited `0`; safe same-value
      rotation plus single-stdin zhangbot token handoff and value-free
      mode/shape check exited `0`.
    - Corrected release archive transfer and remote archive/hash checks exited
      `0`. The first corrected release install command exited `2` before any
      action due a remote shell syntax error; the next command reached
      installation but exited `1` because six hash paths were evaluated from
      `$HOME` rather than the release directory. Its remote release rollback
      trap exited effectively `0`, leaving no release/current/unit/process.
    - Token removal exited `0`; Edge Secret deletion/name readback exited
      `0`; WAF rule deletion/readback exited HTTP `200`/zero rules, entrypoint
      deletion exited HTTP `204`, and final entrypoint readback exited HTTP
      `404`/`10003`. No D1/R2 object, Edge deploy, Redis, Relay, or
      Cloudflared write occurred.

60. Processor listener assertion retry and rollback
    - From backed-up commit `6a70062186b68b2cd4703f762d3a8f62d3b6eb1e`, the
      fresh immutable preflight, exact WAF rule/readback, secret/token handoff,
      archive transfer/hash checks, venv, offline dependency install, import
      check, and systemd start all reached success before the post-start
      listener assertion.
    - The assertion exited `81` because it rejected the pre-existing
      cloudflared loopback metrics listener `127.0.0.1:20242` (and did not
      distinguish existing SSH/DNS listeners). Read-only `ss -ltnp` identified
      `20242` as cloudflared, `8090` as Redis Relay, and `6379` as Redis; no
      socket belonged to the Processor main PID. The failure was therefore in
      the acceptance assertion, not an observed Processor listener.
    - The release trap removed release/current/unit/process. Token removal,
      Edge Secret deletion/name readback, WAF rule deletion/readback, empty
      entrypoint deletion, and final entrypoint readback exited `0`, `0`, HTTP
      `200`/zero rules, HTTP `204`, and HTTP `404`. No D1 migration, R2
      object, Edge deployment, or existing-service write occurred.

59. Corrected release retry smoke-check stop and rollback
    - From backed-up commit `a270b30ad4d144aad5431713d163dee8e7fcac4b`, the
      second exact WAF rule/readback and same-value Edge/zhangbot secret
      handoff passed; the current-commit source archive and remote archive
      hash/member checks also exited `0`.
    - The corrected absolute-path release setup created the venv and installed
      the offline lock, but its import smoke check exited `1` because an SSH
      shell quote changed `print('imports_ok')` into an invalid Python name
      expression (`NameError`). No unit was registered or started.
    - The remote release trap left release/current/unit/process absent. Token
      removal, Edge Secret deletion/name readback, WAF rule deletion/readback,
      empty entrypoint deletion, and final entrypoint readback exited
      `0`, `0`, HTTP `200`/zero rules, HTTP `204`, and HTTP `404` respectively.
      No D1 migration, R2 object, Edge deployment, or existing-service write
      occurred.

61. GitHub backup confirmation and fourth release rollback
    - Local branch/worktree/history check exited `0`; branch was
      `cloudflare-deploy`, worktree clean, and HEAD was
      `becde2db27aa7ef7e31bfb2ddbe4a0f4ce7be8cf`.
    - IPv4 SSH non-force push to the repository's
      `refs/heads/cloudflare-deploy` exited `0` with `[up to date]`; exact
      SSH `ls-remote --heads` readback exited `0` and returned the same SHA.
    - Read-only preflight exited `0` for `wrangler whoami`, remote D1
      migrations list (`No migrations to apply!`), Worker deployments list,
      name-only Edge Secret list, Edge health, token verify, WAF entrypoint
      read, and zhangbot OS/venv/service/listener checks. D1 migrations
      `0017`–`0021` were not rerun.
    - The stale exact WAF rule was deleted with HTTP `200`; empty entrypoint
      deletion returned HTTP `204`, and final readback returned HTTP `404`.
      Edge Secret deletion exited `0` and name-only readback reported zero
      `PAPER_PROCESSOR_SHARED_SECRET` entries. Exact zhangbot token removal
      exited `0`; the file was absent afterward.
    - The zhangbot release install retry exited `1` after printing only the
      systemd user-service symlink creation. Read-only post-failure checks
      showed release/current/unit/process absent; the failure cause was not
      surfaced and is intentionally not guessed. Edge deploy and R2 write
      did not occur. Existing Redis/Relay/Cloudflared checks remained active.
    - Post-rollback Edge health exited `0` and reported
      `paper_processor: unconfigured`. PAPER-10 remains blocked and
      `deployment.txt` is absent.
    - Evidence-only `git diff --check` exited `0`; the changed-scope secret
      detector returned raw no-match exit `1` and normalized gate exit `0`
      when excluding its own detector-record line.

62. Wheel archive metadata stop and rollback
    - Backed-up branch/worktree and current-commit artifact/hash checks exited
      `0`; local source archive member/hash validation exited `0` and found no
      `._*`/`__MACOSX` member. Edge/Processor/frontend local gates were all
      green before the external attempt.
    - Exact WAF rule creation/readback exited HTTP `200`/exactness `0`.
      Secure Edge Secret and zhangbot token handoff exited `0`; token file
      mode/key/shape readback exited `0`.
    - `scp` archive transfer exited `0`. Remote source archive validation
      exited `0`; remote wheel archive validation exited `32` after GNU tar
      reported macOS extended metadata and the strict AppleDouble check
      stopped the release. No release install, service start, Edge deploy, or
      R2 write followed.
    - Capability rollback exited `0`: WAF rule delete HTTP `200`, entrypoint
      delete HTTP `204`, final entrypoint readback HTTP `404`; Edge Secret
      deletion/name-only readback exited `0`/zero entries; exact zhangbot
      token and staging removal exited `0`. Existing Redis/Relay/Cloudflared
      checks remained active.
    - Replacement local wheel archive using explicit files, `ustar`, and
      `COPYFILE_DISABLE=1` passed member validation (exit `0`) with SHA-256
      `6bf318a077fd6e82a12e8a6e70178ea6a64582d002b7ca4f542af4165314ff1c`.

63. systemd verification-order stop and rollback
    - Current `7505d07…` source/wheel archive generation and local
      AppleDouble/hash validation exited `0`; remote transfer and source,
      wheel, source-aggregate, lock, and unit hash validation exited `0`.
    - Exact WAF creation/readback exited HTTP `200`/exactness `0`; secure Edge
      Secret and zhangbot token handoff plus token-file shape readback exited
      `0`.
    - Release extraction, venv/ensurepip/pip, offline dependency installation,
      and install-record preparation reached the systemd verification step.
      `systemd-analyze --user verify` exited `1` with the safe diagnostic that
      `current/.venv/bin/python` did not yet exist; no unit was installed,
      enabled, or started.
    - Capability rollback exited `0`: WAF rule delete HTTP `200`, entrypoint
      delete HTTP `204`, final readback HTTP `404`; Edge Secret deletion and
      name-only readback exited `0`/zero entries; zhangbot release/current/
      unit/token/staging removal completed. The first aggregate rollback
      command returned exit `3` only because the absent Processor unit made
      `systemctl is-active` return its normal inactive status; a follow-up
      non-failing check returned exit `0` with Processor exit `3` and existing
      Cloudflared/Redis/Relay exit `0` active. No D1/R2/Edge deploy occurred.
    - Post-rollback Edge health exited `0` with
      `paper_processor: unconfigured`; `deployment.txt` remains absent.

64. Live acceptance browser blocker and full rollback
    - Backed-up `5d2c128…` archive/hash/member checks, exact WAF create/readback,
      secure Secret/token handoff, Processor release, and Edge deploy all
      exited `0`; Edge version `4d2a792b-a767-4e91-8a66-09aac0c673e9` was
      deployed at 100%.
    - Browser skill setup completed. In-app Browser navigation reported
      `ERR_BLOCKED_BY_CLIENT`; Chrome extension navigation to the production
      root timed out before a DOM snapshot. No browser form, upload, login
      input, or user-data transmission occurred. A separate direct HTTPS root
      read returned 200 HTML only and was not counted as acceptance.
    - Required authenticated live cases were therefore not run and no mock,
      HTTP 200, or process-liveness result was substituted.
    - WAF rule deletion HTTP `200`, entrypoint deletion HTTP `204`, and final
      WAF readback HTTP `404`; Edge Secret deletion/name-only readback exited
      `0`/zero entries; zhangbot stop/disable/daemon-reload exited `0` and
      exact Processor targets were absent. Edge rollback to
      `d287b02d-a94c-4caa-b473-70f2368f4999` exited `0` and read back at 100%.
      D1 migration list exited `0` with no migrations to apply; R2 info exited
      `0` with `15` objects/`41.9 MB`; existing Redis/Relay/Cloudflared were
      active. `deployment.txt` was not created.
    - Final evidence `git diff --check` exited `0`; the changed-scope secret
      detector returned raw no-match exit `1` and normalized gate exit `0`,
      excluding the detector's own pattern record.

65. Kimi provider authenticated preflight and rollback
    - Initial sandboxed `npx wrangler versions list --name
      infinity-agents-edge --json` read-only attempt exited `1` because the
      Cloudflare API hostname could not be resolved in the restricted network.
      The same read-only check under the approved no-proxy network condition
      exited `0` and identified candidate `1f81cfc1-481d-4678-a236-1c854b94b714`
      (version 148) and rollback reference
      `d287b02d-a94c-4caa-b473-70f2368f4999` (version 143).
    - Read-only `npx wrangler deployments list --name infinity-agents-edge
      --json` exited `0`; the Kimi candidate was at 100% before rollback.
    - The authenticated browser text probe used only a harmless fixed marker.
      The application returned real `401 Invalid Authentication`; this required
      text gate failed, so tool and image probes were not attempted.
    - `npx wrangler versions deploy d287b02d-a94c-4caa-b473-70f2368f4999@100
      --name infinity-agents-edge --yes --message "Rollback Kimi provider after
      authenticated 401 probe"` exited `0`. Read-only deployment readback
      exited `0` with the exact rollback version at 100%; public `/health`
      returned HTTP `200`.
    - No D1 migration was rerun. No WAF, R2 object, new Edge Secret, zhangbot
      release/service/token, Redis/Relay/Cloudflared, or live paper workflow
      operation was performed in this subphase. No provider credential value
      was read or recorded.
    - Scoped evidence `git diff --check` exited `0`; the no-secret detector
      returned raw no-match exit `1` and normalized gate exit `0`.

66. Kimi K2.6 browser text-probe blocker and rollback
    - Two fresh `openTabs()` discovery/claim attempts targeted the existing
      authenticated Infinity Agents tab; both were refused by browser-session
      ownership. No provider request was sent, so provider status remained
      `NOT_VERIFIED` and no tool/image test was started.
    - Read-only `npx wrangler deployments list --name infinity-agents-edge
      --json` exited `0`; candidate `93983647-e6f6-4497-a128-2dfd478d15f5`
      was at 100%.
    - `npx wrangler versions deploy d287b02d-a94c-4caa-b473-70f2368f4999@100
      --name infinity-agents-edge --yes --message "Rollback Kimi K2.6 provider
      after browser probe could not be completed"` exited `0`; read-only
      deployment readback exited `0` with the exact rollback version at 100%.
      Public `/health` returned HTTP `200`.
    - No D1 migration, R2 object, WAF, Edge secret, zhangbot Processor,
      Redis/Relay/Cloudflared, or live paper workflow operation occurred.

67. Kimi K2.6 mainland endpoint correction and local gates
    - Red TDD run: `cd cloudflare-worker && npm test -- --run
      test/model-provider.test.ts` exited `1` because the checked-in
      `wrangler.jsonc` still contained the international `.ai` endpoint.
    - Corrected focused run: `npm test -- --run
      test/model-provider.test.ts test/chat.test.ts test/paper-privacy.test.ts`
      exited `0`; 3 files and 26 tests passed.
    - `cd cloudflare-worker && npm run check` first exited `2` on test-only
      typing assertions; after the test fixes it exited `0`. Full
      `cd cloudflare-worker && npm test` then exited `0`; 24 files and 134
      tests passed.
    - `eval "$(pyenv init - zsh)"; pyenv shell Agent; PYTHONPATH=. python -m
      pytest -q tests/test_paper_processor_runtime.py
      tests/test_paper_processor_ingestion.py` exited `0`; 12 tests passed.
    - Frontend `npm run typecheck`, `npm run lint`, and `npm run test:unit`
      each exited `0`; 12 files and 50 unit tests passed. The first
      `npm run test:e2e` exited `1` because sandbox binding to
      `127.0.0.1:3000` returned `EPERM`; the permitted elevated retry exited
      `0` with 13 tests passed.
    - `git diff --check` exited `0`. Read-only Wrangler/HTTPS preflight first
      hit sandbox DNS/log-directory restrictions; the permitted retry returned
      `0` for account/deployment/secrets/D1/R2. The mistaken `/healthz` read
      returned `404`; corrected `/health` returned HTTP `200`, curl exit `0`.
    - No Cloudflare, D1, R2, WAF, Edge Secret, zhangbot, Processor,
      Redis/Relay/Cloudflared, browser, or deployment write occurred in this
      subphase.
68. Kimi K2.6 mainland correction deployment and browser stop
    - `npx wrangler deploy --dry-run --message 'PAPER-10 Kimi K2.6 mainland
      endpoint correction'` exited `0`.
    - `npm run deploy -- --message 'PAPER-10 Kimi K2.6 mainland endpoint
      correction'` exited `0`; Worker version
      `79755db3-c12d-4737-b601-aa99f11e3f93` was read back at 100% under
      deployment `a98fd1bd-ff46-4768-8cd8-af23dcc53fca`.
    - Read-only `/health` returned HTTP `200`, curl exit `0`.
    - Three fresh Chrome `openTabs()`/claim attempts found the exact existing
      authenticated URL/title but each returned the safe blocker
      `tab_owned_by_another_browser_session`; no DOM or provider request was
      obtained.
    - No D1 migration, R2 object, WAF rule, Edge Secret, zhangbot Processor,
      Redis/Relay/Cloudflared, browser data, or GitHub write occurred.

69. Kimi mainland text gate and current WAF rule
    - Coordinator-provided authenticated browser acceptance record: the exact
      prompt `请只回复：KIMI_MAINLAND_TEXT_PROBE_OK` returned the exact visible
      result `KIMI_MAINLAND_TEXT_PROBE_OK` against Worker version
      `79755db3-c12d-4737-b601-aa99f11e3f93`. This is a real acceptance record,
      not a shell test; no credential value or browser secret was recorded.
    - Secure WAF capability preflight: token metadata mode check exit `0`;
      authenticated Rulesets read HTTP `200`, exit `0`; custom entrypoint
      absence read HTTP `404`/API error `10003`, exit `0`.
    - Exact additive zone ruleset creation: HTTP `200`, exit `0`; entrypoint
      `65e15547ea3144feb70791fc155d1df0`, rule
      `5695e7eb000c4f49b77a616cff1411ae`.
    - Immediate exact readback: HTTP `200`, exit `0`; readback validation
      `PASS`; one enabled/logged rule, action `skip`, products `bic`, and the
      fixed source/host/four method-path expression matched the target.
    - No D1 migration was rerun and no Edge secret, Processor token/release/
      service, R2 object, Redis/Relay/Cloudflared write occurred yet.

70. Evidence consistency gate for the current update
    - `git diff --check` exited `0`.
    - The changed-scope no-secret detector over the updated Paper-10 evidence
      files, excluding this detector record, returned raw no-match exit `1`;
      normalized secret-gate exit `0`.

71. Fresh immutable preflight after the Kimi text gate and WAF write
    - Local branch/worktree/hash/AppleDouble checks exited `0`. Read-only
      `git ls-remote --heads origin cloudflare-deploy` exited `0` and returned
      `e551a5994cd228f19a2ae816c4529e4b04cf41a1`; no push was run. The local
      evidence HEAD is `794f8b10d0c86c50a0575cc4fe2869aaa0aa537b`.
    - `npx wrangler whoami`, deployment inventory, secret-name list, D1
      migration list, D1 schema query, R2 bucket list/info, and public health
      reads exited `0`. D1 reported no migrations to apply; schema metadata
      showed all required tables/indexes with zero writes; R2 info was 15
      objects / 41.9 MB; health was HTTP `200` with Processor unconfigured.
    - WAF token mode metadata read exited `0`; secure current-rule curl exited
      `0` and semantic exactness validation exited `0`. Readback was HTTP
      `200`, entrypoint `65e15547ea3144feb70791fc155d1df0`, rule
      `5695e7eb000c4f49b77a616cff1411ae`, exact fixed expression, `skip` only
      `bic`, enabled, and logged.
    - Corrected zhangbot SSH read-only checks exited `0` for package, venv,
      ensurepip, pip, service/listener/state inspection, and source checks.
      Three egress providers returned `39.105.204.121`; the fourth candidate
      timed out with curl exit `28` and was not used as a pass, while the
      independent three-provider agreement passed. The first remote process
      probe used unavailable `rg` and returned `127`; a grep-based clean scan
      returned no Processor match. No service was stopped or changed.
    - No D1 migration, R2 object, Edge shared secret, Processor token/release/
      service, Redis/Relay/Cloudflared, or Edge code write occurred during
      this preflight. The next authorized write is the secret/token handoff.

73. Secret handoff wrapper failure and capability-first rollback
    - Edge Secret write exited `0`; name-only readback exited `0` and showed
      the expected binding. The SSH token handoff exited `1` before writing:
      the remote `set -e` stopped on EOF from the no-newline stdin value before
      the intended read-status guard. Post-state SSH read exited `0` with the
      token env absent and Processor service inactive.
    - WAF rule deletion exited `0`/HTTP `200`; empty-entrypoint deletion curl
      exited `0` but its JSON-body validator exited `1` on the successful empty
      HTTP `204` response. A separate entrypoint readback exited `0`/HTTP
      `404`. Edge Secret deletion/name-only absence exited `0`; zhangbot
      staging cleanup and local temporary-secret removal exited `0`.
    - No Processor release/unit/process, D1 migration, R2 object, Edge code,
      Redis/Relay/Cloudflared, or live Paper operation followed.

74. Handoff rollback evidence consistency gate
    - The rollback evidence records only safe IDs, statuses, exit codes, and
      absence facts. No token, secret value, authorization header, or payload
      was emitted. The next attempt must use a fresh preflight.

72. Preflight evidence consistency gate
    - `git diff --check` exited `0`.
    - The changed-scope no-secret detector over the updated PAPER-10 evidence,
      excluding `secret-scan.txt` itself, returned raw no-match exit `1` and
      normalized secret-gate exit `0`.

75. Fresh immutable preflight after rollback and Kimi mainland text PASS
    - Local branch/status/hash and tracked AppleDouble scan exited `0`; local
      HEAD was `1fc350e7a80c5e330492e235641dea477eb88bee` and read-only
      `git ls-remote --heads origin cloudflare-deploy` exited `0`, returning
      `e551a5994cd228f19a2ae816c4529e4b04cf41a1`. No push was run.
    - Source aggregate, dependency lock, and service-unit hash checks exited
      `0` and matched the checked-in delivery manifest. The exact authenticated
      mainland Kimi browser result remains the recorded visible
      `KIMI_MAINLAND_TEXT_PROBE_OK` against Worker version
      `79755db3-c12d-4737-b601-aa99f11e3f93`; read-only current-version
      inspection confirmed the `.cn` base URL and `kimi-k2.6` model.
    - Read-only `npx wrangler whoami`, deployment/version inspection,
      secret-name list, D1 migration list, D1 schema query, R2 list/info,
      and public health exited `0`; health curl was HTTP `200`. D1 reported
      `No migrations to apply!`, schema metadata reported `changes=0`,
      `changed_db=false`, and `rows_written=0`; R2 remained `15 objects / 41.9
      MB`; Paper shared-secret name was absent.
    - Secure WAF token metadata exited `0` with mode `0600`; token verify was
      HTTP `200`/active, Rulesets read was HTTP `200` with four non-custom
      rulesets, and post-rollback custom-entrypoint read was HTTP `404`/10003.
      No WAF rule was active during this preflight.
    - Read-only zhangbot SSH exited `0` for OS/package/runtime checks,
      venv help/creation/ensurepip/pip, existing Redis/Relay/Cloudflared
      active checks, listener inventory, and source checks. The first egress
      probe script was not used as a pass because its final `tr` masked curl
      failures; the corrected probe returned `39.105.204.121` from
      `icanhazip`, `ipinfo`, `checkip.amazonaws.com`, and
      `ipv4.icanhazip.com`. `api.ipify.org` failed with curl `7`,
      `ifconfig.me` timed out with `28`, and `ip.seeip.org` timed out with
      `28`; these were recorded as non-passes. The anchored Processor scan
      exited `1` (no runner), and the release-children find exited `0` after
      the stale release was isolated.
    - The inert old release was moved to the exact 0700 quarantine path with
      SSH move exit `0`, chmod/readback exit `0`, and formal release-root
      empty check `1` (empty=true). No existing service, token, current
      symlink, D1/R2 object, or Edge deployment was changed.

76. Fresh preflight consistency gate
    - The new preflight evidence contains only public model/path facts, safe
      target IDs, artifact hashes, rule capability/status, exit codes, and
      absence/readiness facts. No credential value, authorization header,
      cookie, paper bytes, image bytes, R2 key, or full provider payload was
      emitted.

77. Fresh preflight working-tree evidence gate
    - `git diff --check` exited `0`.
    - The scoped secret-value detector over all updated PAPER-10 evidence,
      excluding `secret-scan.txt` itself, returned raw no-match exit `1`; the
      normalized secret gate exited `0`. The detector allows documented secret
      names but rejects credential values, bearer values, private keys,
      credential-bearing URLs, and secret assignments.

78. Exact WAF rule retry and semantic readback
    - The first corrected-endpoint create request returned HTTP `400`, curl
      exit `0`, API error `20127`; JSON parsing exited `0`. A post-failure
      entrypoint read returned HTTP `404`, curl exit `0`, error `10003`, proving
      no partial WAF state.
    - The second create request used the same four fixed method/path pairs and
      only adjusted the documented inline-set whitespace. Payload build and
      JSON parse exited `0`; create returned HTTP `200`, curl exit `0`, and
      returned entrypoint `6a212d8fb2444135a6b2511e7d8ad8d0` and rule
      `a7f6a28a87624da28d595a11eeb5d92b`.
    - Immediate readback returned HTTP `200`, curl exit `0`; the first
      validator invocation failed locally with a JavaScript shell-quoting
      compile error and was not treated as acceptance. The corrected
      value-free validator then returned exit `0` and `validation=PASS` for
      exact phase/kind, one rule, rule ID, expression, enabled/logged state,
      `skip`, exactly `products=["bic"]`, and no wildcard.
    - No D1 migration, R2 object, Edge Secret, Processor token/release/service,
      Redis/Relay/Cloudflared change, or Edge code deployment followed the WAF
      readback. The next external write is the secret/token handoff.

79. WAF retry evidence consistency gate
    - The WAF retry evidence contains only safe IDs, expression scope, public
      paths, statuses, and exit codes. No WAF token, provider credential,
      shared secret, Processor token, cookie, paper bytes, or raw payload was
      emitted.

80. WAF retry working-tree evidence gate
    - `git diff --check` exited `0`.
    - The scoped secret-value detector over all updated PAPER-10 evidence,
      excluding `secret-scan.txt`, returned raw no-match exit `1`; normalized
      secret gate exit `0`.

86. Archive-precondition rollback working-tree gate
    - `git diff --check` exited `0`.
    - The scoped secret-value detector over all updated PAPER-10 evidence,
      excluding `secret-scan.txt`, returned raw no-match exit `1`; normalized
      secret gate exit `0`.

84. Current-HEAD archive precondition stop and rollback
    - Local expected archive paths for current HEAD `5c4b7563…` were absent;
      only prior-commit `5e42e4e…` archives existed. The precondition exited
      `2`, so no `scp`, staging transfer, or Processor install followed.
    - Capability-first WAF rule delete returned HTTP `200`/curl exit `0`,
      entrypoint delete HTTP `204`/curl exit `0`, and final entrypoint readback
      HTTP `404`/curl exit `0`. zhangbot token and staging cleanup each exited
      `0`.
    - The first Edge Secret delete returned exit `1` because Wrangler's
      `secret delete` has no `--yes` option; no deletion was assumed. The
      supported confirmation-input retry exited `0`; name-only secret read
      exited `0` and showed `present=0`. No secret value was printed.
    - No Processor release/unit/process, D1 migration, R2 object, Edge code,
      Redis/Relay/Cloudflared, or live Paper operation occurred.

85. Archive-precondition rollback evidence gate
    - Evidence contains only commit prefixes, safe resource IDs, status/exit
      codes, and absence facts. No secret, token, authorization header,
      provider key, cookie, paper/image bytes, or raw payload was emitted.

81. Edge shared-secret and zhangbot token handoff
    - The first local generator precondition returned exit `3` because the
      newline-terminated value was 65 bytes; no Cloudflare or zhangbot write
      followed. Corrected strict generator exited `0`, chmod exited `0`, and
      byte-count validation returned `64`.
    - `npx wrangler secret put PAPER_PROCESSOR_SHARED_SECRET` exited `0`;
      name-only `secret list` exited `0` and found the binding. The secure SSH
      handoff exited `0`; remote `read` returned expected EOF exit `1` with a
      non-empty value, remote file validation reported mode `600`, one line,
      and shape `PASS`. Independent token post-read exited `0` with the same
      safe metadata. No value was printed or recorded.
    - Edge health post-read curl exited `0`, HTTP `200`, and reported
      `paper_processor=configured`; local temporary-secret removal exited `0`.
      zhangbot post-read showed Processor inactive/no runner and existing
      Redis/Relay/Cloudflared active. No D1 migration, R2 object, Edge code
      deployment, or existing-service write followed.

82. Capability-handoff evidence gate
    - This handoff evidence contains only the 65-byte precondition fact, safe
      statuses, exit codes, metadata/shape results, and absence/readiness
      facts. No shared secret, token, authorization header, provider key,
      cookie, paper bytes, image bytes, or full payload was emitted.

83. Capability-handoff working-tree evidence gate
    - `git diff --check` exited `0`.
    - The scoped secret-value detector over all updated PAPER-10 evidence,
      excluding `secret-scan.txt`, returned raw no-match exit `1`; normalized
      secret gate exit `0`.

87. Archive-precondition rollback evidence commit gate
    - Current branch is `cloudflare-deploy`; current HEAD before this evidence
      commit is `5c4b7563…`; the read-only remote ref is `e551a599…`.
    - The scoped secret-value detector over the nine updated PAPER-10 evidence
      files, excluding `secret-scan.txt`, returned raw no-match exit `1`; the
      normalized secret gate exited `0`.
    - `git diff --check` exited `0`. No secret value, token, authorization
      header, provider credential, cookie, paper/image bytes, or raw payload
      was emitted.

88. Current-HEAD archive rebuild and immutable preflight
    - Repository check exited `0`: branch `cloudflare-deploy`, clean HEAD
      `63cb12c2…`. Read-only no-proxy `git ls-remote --heads origin
      cloudflare-deploy` exited `0` with remote `e551a599…`; no GitHub write.
    - Source and wheel archive creation exited `0`. Source/wheel tar listing
      each exited `0`; no-AppleDouble scans each returned expected rg exit
      `1`; required source/unit/lock and wheel member checks exited `0`.
      Manifest source/lock/unit comparisons exited `0`; all three wheel
      lock-hash comparisons exited `0`.
    - `npx wrangler --version` exited `0` (`4.120.0`); `whoami` exited `0`;
      deployment status exited `0` with current version
      `5b7c9f4b…` at 100%; version binding read exited `0` and showed the
      mainland Kimi endpoint/model and fixed Processor bindings without a
      credential value. Secret-name list exited `0` with no
      `PAPER_PROCESSOR_SHARED_SECRET`.
    - D1 migration listing exited `0` with `No migrations to apply!`; the
      read-only schema query exited `0` with all required tables/indexes and
      `changes=0`, `changed_db=false`, `rows_written=0`. R2 list/info each
      exited `0` with 15 objects / 41.9 MB. Health curl exited `0`/HTTP `200`
      with Processor unconfigured.
    - Secure WAF Rulesets read exited `0`/HTTP `200`; custom entrypoint read
      exited `0`/HTTP `404`. WAF token metadata reported mode `0600` and no
      value was emitted.
    - SSH preflight exited `0`: package dpkg/candidate both exact
      `3.10.12-1~22.04.17`; venv help/create/ensurepip/pip/cleanup all exited
      `0`; Redis/Relay/Cloudflared active/enabled; Processor unit/token/current
      /release/runner absent; no Processor listener; fixed egress and arXiv/
      PMC checks exited `0` with expected HTTP results.

90. Exact fixed-endpoint WAF rule recreate and readback
    - WAF request body validation exited `0`; create curl exited `0` with HTTP
      `200`, producing entrypoint `4eee604b…` and rule `08b7fc2e…`.
    - Immediate readback curl exited `0` with HTTP `200`; the value-free
      semantic validator exited `0` and verified one enabled/logged zone
      custom rule, `skip` with `products=["bic"]` only, no phase skip, and
      the exact four fixed method/path pairs. No wildcard or broad exception
      was present. The WAF token value was never emitted.

92. Processor install hash-check stop and capability-first rollback
    - Transfer mkdir/source-scp/wheels-scp and remote archive SHA/member
      validation each exited `0`. Installation stopped at `step=extracted_hash`:
      source comparison exited `1`, lock/unit comparisons exited `0` because
      the installer aggregated absolute paths instead of the manifest-relative
      paths. Venv, pip, import, unit registration, and service start did not
      run.
    - Rollback exited `0`: WAF rule delete HTTP `200`, entrypoint delete HTTP
      `204`, final readback HTTP `404`; Edge secret delete/name-only absence
      both exited `0`; zhangbot cleanup exited `0` with new paths absent and
      existing Redis/Relay/Cloudflared active. No D1/R2/Edge deploy occurred.
    - Retry changes only the hash working directory to the release root before
      repeating the exact manifest command; a fresh artifact/preflight is
      required before any new capability write.

94. Fresh preflight after relative-path hash correction
    - Branch/worktree/head/remote checks exited `0`: clean `cloudflare-deploy`,
      HEAD `9e5de68d…`, read-only remote `e551a599…`.
    - Current-HEAD archive creation and SHA output exited `0`; source/wheel tar
      listing and required member checks exited `0`; no-AppleDouble scans
      returned expected rg exit `1`; manifest source/lock/unit and pinned-wheel
      hash checks exited `0`.
    - Wrangler account/deployment/version/secret-name/D1 migration/schema/R2
      reads all exited `0`; current 100% version is `dc40c31d…` with mainland
      Kimi and fixed Processor bindings, Paper secret is absent, D1 reports no
      pending migrations and `changes=0`, `changed_db=false`, `rows_written=0`,
      R2 remains 15 objects/41.9 MB, and health curl exited `0`/HTTP `200`.
    - WAF token metadata/Rulesets reads exited `0`, with entrypoint HTTP `404`;
      SSH runtime/package/venv/ensurepip/pip/service/listener/staging/runner
      checks exited `0`, fixed egress/source checks passed. No write occurred.

95. Transfer command stop and capability-first recovery
    - Staging mkdir/source SCP/wheel SCP each exited `0`; remote validation
      exited `255` before execution because SSH parsed the environment
      assignment as a hostname. No release install ran.
    - WAF was already absent; Edge secret-name read exited `0` and showed
      absent; zhangbot token/staging/release/current/unit cleanup exited `0`
      and final absence checks passed. Existing Redis/Relay/Cloudflared were
      active. No D1/R2/Edge deployment occurred.
    - Retry changes only SSH argument ordering and requires fresh preflight.

93. Processor install failure evidence gate
    - `git diff --check` exited `0`.
    - The scoped secret-value detector over updated PAPER-10 evidence,
      excluding `secret-scan.txt`, returned raw no-match exit `1`; normalized
      secret gate exited `0`.

91. WAF evidence working-tree gate
    - `git diff --check` exited `0`.
    - The scoped secret-value detector over updated PAPER-10 evidence,
      excluding `secret-scan.txt`, returned raw no-match exit `1`; normalized
      secret gate exited `0`.

89. Current-HEAD preflight evidence working-tree gate
    - `git diff --check` exited `0`.
    - The scoped secret-value detector over all updated PAPER-10 evidence,
      excluding `secret-scan.txt`, returned raw no-match exit `1`; normalized
      secret gate exited `0`.

96. Post-recovery fresh preflight
    - Clean `cloudflare-deploy` HEAD/remote and archive presence checks exited
      `0`; manifest source/lock/unit and no-AppleDouble checks passed.
    - Current account/target, active 100% Worker version/bindings, secret-name,
      D1 migration/schema, R2, and health read checks all exited `0`; D1
      reported no pending migrations with zero changes/writes and health was
      HTTP `200` with Processor unconfigured.
    - WAF token metadata/Rulesets reads exited `0` with the fixed entrypoint
      HTTP `404`; SSH package/venv/ensurepip/pip/service/listener/staging/
      runner and fixed-source checks exited `0`. No write occurred.

97. Latest Processor install stop, rollback, and Kimi mainland gate
    - The real authenticated browser probe against Worker version
      `79755db3-c12d-4737-b601-aa99f11e3f93` rendered the exact marker
      `KIMI_MAINLAND_TEXT_PROBE_OK`; no credential value was captured.
    - The corrected archive transfer and relative source/lock/unit hash checks
      passed. The subsequent no-AppleDouble guard invoked `rg` on zhangbot,
      which is not installed; that guard exited `127`, so installation stopped
      before venv/release/unit creation. This condition is being changed to the
      already available `grep` utility before any retry.
    - Capability-first rollback read back WAF HTTP `404`, Paper shared-secret
      name absent, zhangbot token/staging/release/current/unit absent, and
      runner count `0`; SSH read-only status exited `0` and existing
      Redis/Relay/Cloudflared remained active. No D1 migration was rerun and no
      R2 write or Edge code deployment occurred.

98. Current fresh preflight and local gates after mainland Kimi gate
    - Branch/HEAD/scope checks exited `0`: `cloudflare-deploy`, HEAD
      `9e5de68d…`, only PAPER-10 evidence changes; read-only remote ref
      `e551a599…`. Source archive SHA-256
      `c440b41c1f6bd64f7a6c5d3f79e7ed0830860039b2b25ffef395bcd1ffd5cd5d` and
      wheel archive SHA-256
      `3294170f5e7d8a6d7d68aec3951f86b8dfc9a6e9a68ef11b631d67b3f872d6ab`
      matched the commit-named artifacts. Manifest hash comparisons and exact
      source/wheel member checks exited `0`; AppleDouble scans returned the
      expected no-match exit `1`; `git diff --check` exited `0`.
    - Cloudflare `whoami`, deployment status/version read, secret-name read,
      D1 migration/schema read, corrected R2 info, health, and WAF Rulesets
      reads exited `0`. The explicit-APAC R2 CLI attempt returned `400 invalid
      jurisdiction` and was not used as the preflight result; the corrected
      default-jurisdiction read returned APAC/15 objects/41.9 MB. Active Worker
      is `f8470d1f…` (#161), mainland Kimi bindings are present, Paper secret
      name is absent, health is HTTP `200`, and WAF custom entrypoint is absent
      (`404`). D1 reported no pending migrations and zero changes/writes.
    - zhangbot SSH read-only checks exited `0`: exact dpkg status/candidate
      `3.10.12-1~22.04.17`, venv/ensurepip/pip, fixed egress
      `39.105.204.121`, arXiv/PMC HTTP `200`, existing services/listeners, and
      no Processor state. The first dpkg quoting probe and `ifconfig.me` probe
      were corrected; the corrected package/egress results exited `0`.
    - `cd cloudflare-worker && npm run check && npm test` exited `0` (24 files,
      134 tests); Processor pytest exited `0` (12 passed); frontend typecheck,
      lint, and unit exited `0` (50 tests); first sandbox E2E exited `1` on
      local bind EPERM, then elevated `npm run test:e2e` exited `0` (13 passed).
      Warnings were non-fatal and no test was skipped or weakened.
    - The real authenticated text probe had already rendered exactly
      `KIMI_MAINLAND_TEXT_PROBE_OK` against Worker version
      `79755db3-c12d-4737-b601-aa99f11e3f93`; no credential value was captured.
      No external write occurred in this preflight.

99. Exact fixed-endpoint WAF create/readback
    - The first additive body returned HTTP `400`/`unknown field version`;
      corrected read-only inventory returned HTTP `200` with no partial custom
      entrypoint. The corrected create returned HTTP `200`, entrypoint
      `eec4070b…`, rule `026d95bb…`; immediate readback returned HTTP `200`.
    - Semantic validator exited `0`: one enabled/logged zone custom rule in
      `http_request_firewall_custom`, action `skip`, action keys exactly
      `products`, products exactly `["bic"]`, exact source IP/host and the
      three POST plus one PUT fixed paths. No wildcard or broad bypass.
    - This is the only current external change. Next is paired secret/token
      handoff; WAF rule and entrypoint are the rollback handles. No D1
      migration, R2, Processor, Edge code, or Redis/Relay/Cloudflared write
      occurred.

100. Processor transfer command precondition stop and rollback
     - Remote staging mkdir/source SCP/wheel SCP each exited `0`; remote
       validation exited `255` before execution because environment assignments
       preceded the SSH host (`hostname contains invalid characters`). No
       release or service installation ran.
     - WAF rule/entrypoint deletion returned HTTP `200`/`204`; Edge secret
       deletion exited `0` and the name-only list showed absence. The first
       token cleanup path was expanded locally and did not remove the remote
       file; corrected absolute-path cleanup exited `0`, read back absence, and
       staging/release/current/unit/runner were absent. Existing services stayed
       active. No D1 migration or R2/Edge-code write occurred.
     - The next retry changes SSH ordering only; the failed ordering is not
       repeated.

106. Processor manifest identity check stop and rollback
     - Remote archive SHA/wheel SHA/member/AppleDouble checks passed; extracted
       source aggregate, lock, and unit comparisons each exited `0`. The
       installer then stopped at an incorrect check requiring release HEAD
       `9e5de68d…` in the manifest; the manifest correctly pins reviewed
       Processor source commit `455ae849…`. No venv/service/runner started.
     - Capability-first WAF rule/entrypoint deletion returned HTTP `200`/`204`,
       Edge secret deletion exited `0`, zhangbot cleanup exited `0`, and
       readbacks showed no new Processor state. Existing services stayed
       active; no D1 migration or R2/Edge-code write occurred.
     - The bounded fix compares the reviewed manifest source commit separately
       from the release commit and must repeat a fresh preflight.

101. Retry preflight after transfer rollback
     - Local branch/HEAD/scope/archive/diff checks exited `0`; the current
       commit-named source/wheel archive hashes remained
       `c440b41c…`/`3294170f…` and no-AppleDouble checks returned expected
       no-match exit `1`. Read-only remote ref remained `e551a599…`.
     - Cloudflare deployment/version, secret-name, D1 migration/schema, R2,
       health, and WAF inventory reads exited `0`; active Worker
       `52860ea1…` (#163) retained mainland Kimi/fixed Processor bindings,
       Paper secret was absent, health was HTTP `200`/unconfigured, WAF custom
       entrypoint was absent, D1 had no pending migrations/zero writes, and R2
       was APAC/15 objects/41.9 MB.
    - Corrected zhangbot SSH preflight exited `0`: exact dpkg status and apt
      candidate, venv/ensurepip/pip, fixed egress `39.105.204.121`, arXiv/PMC
      HTTP `200`, existing services/listeners, and no Processor state. No
      external write occurred in this retry preflight.

102. Host-first transfer reached archive-hash guard and rolled back
     - Remote staging mkdir/source SCP/wheel SCP exited `0`; host-first SSH
       validator exited `1` because the expected source aggregate hash was
       accidentally supplied for the archive file. Remote archive SHA was
       `c440b41c…`, while the source aggregate is `510715c4…`; wheel archive
       SHA matched `3294170f…`. No extraction, release, venv, or unit install
       ran.
     - WAF rule/entrypoint deletion returned HTTP `200`/`204`; Edge secret
       deletion exited `0`; zhangbot token/staging cleanup exited `0`; readback
       showed no new Processor state and existing services active. No D1
       migration or R2/Edge-code write occurred.
     - The next validator keeps separate `P10_ARCHIVE_SHA` and
       `P10_SOURCE_SHA` values and repeats fresh preflight; the prior variable
       mix-up is not repeated.

105. Immutable Processor archive transfer with separated archive/source hashes
     - Host-first remote staging mkdir, source SCP, wheel SCP, and archive
       validator exited `0`. Archive SHA matched `c440b41c…`; wheel SHA
       matched `3294170f…`; no-AppleDouble checks reported absent; and all
       required source/manifest/unit/lock/wheel members matched.
     - No extraction, release, venv, unit, service, or runner was created in
       this transfer step. Current WAF entrypoint/rule `421a5184…`/`6dc66d74…`
       and paired capabilities remain the only Paper external changes. No D1
       migration or R2 write occurred.

103. Fresh preflight after archive/source hash rollback
     - Local branch/HEAD/scope/archive/diff checks exited `0`; HEAD remained
       `9e5de68d…`, remote ref `e551a599…`, archive SHA values remained
       `c440b41c…`/`3294170f…`, and no-AppleDouble/member checks passed.
     - Cloudflare deployment/version, secret-name, D1 migration/schema, R2,
       health, and WAF inventory reads exited `0`. Active Worker
       `e281e88c…` (#165) retained mainland Kimi/fixed Processor bindings;
       Paper secret and custom WAF entrypoint were absent; health was HTTP
       `200`/unconfigured; D1 had no pending migrations/zero writes; R2 was
       APAC/15 objects/41.9 MB.
     - Corrected zhangbot read-only checks exited `0`: exact dpkg status and
       apt candidate, venv/ensurepip/pip, fixed egress `39.105.204.121`,
       arXiv/PMC HTTP `200`, existing services/listeners, and no Processor
       state. No external write occurred in this preflight.

107. Latest offline pip install stop and rollback
     - Immutable archive transfer and archive/member/source/lock/unit/manifest
       identity guards exited `0`. The offline pip install exited `1` with
       redacted output `ERROR: Could not find a version that satisfies the
       requirement pypdf==6.15.0 (from versions: none)` because the wheel tar
       had not been extracted into the `--find-links` directory. No venv,
       release, unit, or runner was created.
     - Capability-first rollback exited cleanly: WAF rule and entrypoint
       deletes returned HTTP `200`/`204`; Edge secret delete and zhangbot
       token/staging/release/current/unit cleanup exited `0`; absence and
       existing-service readbacks passed. No D1 migration, R2 object, or Edge
       code deployment occurred.

108. Fresh read-only preflight after pip rollback
     - Local branch/HEAD/scope/archive/hash reads exited `0`; branch is
       `cloudflare-deploy`, HEAD `9e5de68d…`, remote ref `e551a599…`, and only
       PAPER-10 evidence files are dirty. No source or lock change occurred.
     - Corrected Wrangler account/deployment/version/secret-name/D1 migration/
       schema/R2/health reads exited `0`; active version is
       `b70253c2-fe3a-4556-9667-fcbb3c0df053` (#169) with mainland Kimi and
       fixed Processor bindings, Paper secret is absent, D1 has no pending
       migrations and zero writes, R2 is APAC/15 objects/41.9 MB, health is
       HTTP `200`/unconfigured, and WAF Rulesets/entrypoint reads are HTTP
       `200`/`404` respectively.
     - zhangbot SSH package/venv/ensurepip/pip/service/listener/absence and
       source-egress checks exited `0`; existing Redis/Relay/Cloudflared are
       active/enabled and no Processor runner/state exists. The first wrapper
       used the WAF token with Wrangler and failed auth (exit `1`), and the
       first D1 read used the wrong directory (exit `1`); both were corrected
       without any write. D1 migrations `0017`-`0021` were not rerun.

109. Exact capability handoff failure and rollback
     - Exact WAF create/readback exited `0` with entrypoint
       `2e8384f5…`, rule `d2151509…`; Edge secret put/name read exited `0`.
       The SSH token handoff exited `1` before successful env-file validation.
       No token value was printed or recorded.
     - Capability-first rollback deleted the WAF rule/entrypoint with HTTP
       `200`/`204`, Edge secret deletion exited `0`, and zhangbot token/staging
       cleanup exited `0`; final absence reads and health HTTP `200` passed.
       Existing Redis/Relay/Cloudflared stayed active. No D1 migration, R2
       object, or Edge-code deployment occurred.
     - The retry changes only handoff transport/validation: direct stdin to a
       temporary file followed by portable `grep` and line-count checks before
       atomic rename.

110. Archive transfer validator syntax stop and rollback
     - Local staging, source SCP, wheel SCP, and remote archive/wheel SHA
       checks exited `0`; the generated remote member/AppleDouble command
       returned exit `2` with a bash syntax error from nested quoting. No
       extraction or install ran.
     - WAF rule/entrypoint deletion returned HTTP `200`/`204`; Edge secret
       deletion, zhangbot token/staging cleanup, and final absence reads all
       exited `0`. Health was HTTP `200`/unconfigured and existing services
       stayed active. No D1 migration, R2 write, or Edge deployment occurred.
    - The next retry changes only the remote validator representation to a
      literal, hard-coded host-first command and repeats fresh preflight.

111. Archive wheel member-name stop and rollback
     - Staging, both SCP transfers, archive/wheel SHA checks, and AppleDouble
       checks exited `0`; the exact member guard exited `1` because wheel tar
       entries have a leading `./`. No extraction or installation ran.
     - WAF rule/entrypoint deletion returned HTTP `200`/`204`; Edge secret,
       zhangbot token/staging cleanup, absence reads, and existing-service
       checks exited `0`. No D1 migration, R2 object, or Edge deployment
       occurred.
    - The retry normalizes only the wheel tar member prefix and repeats the
      fresh preflight/capability sequence.

112. Latest archive-member rollback and fresh preflight
     - The archive/wheel SHA and AppleDouble checks passed; the wheel member
       guard exited `1` because all three wheel tar members include `./`.
       Capability-first WAF/secret/token/staging rollback and readbacks exited
       `0`/HTTP `200`/`204` as applicable; no install ran.
     - Fresh local, Wrangler, D1, R2, WAF, health, and zhangbot preflight
       reads exited `0`; active Worker `3dda1a0e…` (#175) retained mainland
       Kimi/fixed bindings, D1 had no pending migrations and zero writes, R2
       was APAC/15 objects/41.9 MB, and Processor state was absent.
    - D1 migrations `0017`-`0021` were not rerun. Next retry only normalizes
      the documented `./` wheel member prefix before locked installation.

113. Temporary diagnostic wrapper invalidated
     - Diagnostic directory initialization and both SCP transfers exited `0`,
       but the diagnostic script removed that directory before reading the
       copied archives. Its subsequent missing-file/venv output is invalid.
     - Cleanup exited `0`; no production release, service, WAF, secret, D1,
       R2, or Edge-code mutation occurred. The next diagnostic preserves its
       input directory and adds per-step markers.

114. Production install early stop and corrected temporary diagnostic
     - The production install wrapper exited `1` before its first install
       marker; rollback and path-permission readbacks exited `0`. The first
       temporary diagnostic was invalidated by self-deleting its input.
     - The corrected temporary diagnostic passed archive SHA/AppleDouble,
       source extraction/relative hashes/manifest, wheel extraction/member
       checks, Python 3.10 venv/ensurepip, and offline locked pip (exit `0`);
       no production release or service was created.
     - The next install wrapper adds stage markers only and retains the
       proven wheel extraction. D1 migrations `0017`-`0021` are not rerun.

116. Production archive-root assertion stop and rollback
     - Marked install input and release-parent checks exited `0`; the next
       release-directory assertion exited `1` because the tar root is
       `infinity-paper-processor-<commit>` while the required release path
       is commit-only. No venv/unit/service installation ran.
     - WAF/Edge secret/zhangbot rollback and absence readbacks passed;
       health was `200`/unconfigured and existing services stayed active.
       No D1 migration, R2 object, or Edge-code deployment occurred.
     - The retry validates the root in a temporary extraction directory and
       atomically moves it to the commit-only release directory.

115. Marked-install retry preflight
     - Corrected temporary diagnostic archive/member/source/manifest/wheel/
       venv/ensurepip/offline-pip checks exited `0` and cleanup completed.
     - Fresh local/Wrangler/D1/R2/WAF/health/zhangbot preflight reads exited
       `0`; active Worker `816e5144…` (#177) retained mainland Kimi/fixed
       bindings, D1 had no pending migrations and zero writes, R2 was APAC/
       15 objects/41.9 MB, capabilities were absent, and Processor state was
       absent. D1 migrations were not rerun.

117. Kimi mainland gate and latest Processor post-start stop
     - The real authenticated browser probe on Worker version
       `79755db3-c12d-4737-b601-aa99f11e3f93` submitted the exact Chinese
       text-only prompt and rendered the exact marker
       `KIMI_MAINLAND_TEXT_PROBE_OK`; this is a provider-gate PASS. No secret
       value was read or recorded and StepFun was not used.
     - Latest install stages through `systemd --user start` exited `0`; the
       wrapper then returned `remote_install_rc=1` before a service-readback
       pass marker. WAF rule/entrypoint rollback returned HTTP `200`/`204`,
       Edge secret deletion and host cleanup returned `0`, and post-rollback
       absence reads passed. No D1 migration, R2 object, or Edge deployment
       occurred.
     - Current read-only recovery: secret list has no Paper secret, WAF
       custom-entrypoint list is empty, health is HTTP `200`/unconfigured,
       D1 schema read has zero writes, R2 is APAC/15 objects/41.9 MB, zhangbot
       has no Processor token/unit/current/release/runner, and existing
       services/listeners are unchanged. The next retry must capture active,
       enabled, MainPID, and runner-count values before asserting them.

118. Capability handoff shape rejection and rollback
     - Fresh WAF create/readback returned success; Edge shared-secret put and
       name-only readback returned exit `0`. The zhangbot handoff validator
       returned exit `1` because the temporary env file had
       `line_count=2` from the transport final newline; value shape and
       temporary mode `600` were valid, so installation was safely refused.
     - WAF rule/entrypoint deletion returned HTTP `200`/`204`; Edge secret
       deletion and host token cleanup returned exit `0`. Final reads showed
       no WAF entrypoint, Paper secret, Processor token/service/runner, and
       existing services remained active. No D1 migration, R2 object, or Edge
       deployment occurred.
     - Next retry changes only stdin newline normalization and keeps the
       one-line/one-key/mode-`600` checks.

119. Processor health subphase — Cloudflare secret capability stop
     - `git status --short --branch`: exit `0`; branch was
       `cloudflare-deploy` and the preflight worktree was clean.
     - zhangbot SSH read-only OS/service/venv/listener check: exit `0`.
       `python3.10-venv` was installed at `3.10.12-1~22.04.17`; venv,
       ensurepip, and pip passed; Processor state was absent and existing
       services were active.
     - Edge health from zhangbot: curl exit `0`, HTTP `200`,
       `paper_processor=unconfigured`.
     - Current-HEAD archive construction and SHA/member/AppleDouble checks:
       exit `0`; all three locked wheel SHA checks matched.
     - Edge `npm run check`: exit `0`; Edge `npm test`: exit `0`, 24 files /
       134 tests; Processor focused pytest: exit `0`, 12 passed.
     - Frontend typecheck/lint/unit: exits `0`; frontend E2E initial sandbox
       bind attempt: exit `1` (`EPERM` on `127.0.0.1:3000`); restricted local
       retry: exit `0`, 13 passed.
     - Read-only Worker-secrets capability probe: HTTP `403`, API error
       `10000`, command exit `0` after response classification. No secret
       value was printed or recorded. Wrangler migration read was not
       authenticated in this shell (exit `1` before request); no migration
       write was run.
     - No Clash or external write occurred. Stop status is
       `BLOCKED_CLOUDFLARE_WORKER_SECRET_CAPABILITY`.
