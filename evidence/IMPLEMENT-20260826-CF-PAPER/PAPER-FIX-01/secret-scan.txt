# PAPER-FIX-01 secret and diff scan

The scan scope is limited to the changed source, test, migration, governing
documents, and this card's evidence. Secret values were not read or emitted.

- Corrected changed-scope scan command: `rg -n -i --pcre2 --
  '<credential/private-key/Bearer/embedded-secret patterns>' <explicit
  changed paths>` returned exit `1`, meaning no matches; normalized scan
  result: `PASS`.
- The first invocation omitted the `--` end-of-options marker while the first
  pattern begins with dashes, so `rg` returned exit `2` and performed no scan.
  It was corrected immediately; that diagnostic error is not treated as a
  clean scan.
- `git diff --cached --check`: exit `0` after the evidence EOF fix.
- The same no-match scan was rerun over the staged changed paths, including
  the force-added migration covered by the repository's broad
  `cloudflare-worker/` ignore rule: raw exit `1`, normalized result `PASS`.
- No token, API key, shared secret, private key, authorization value, or
  credential-bearing URL was read, printed, copied, or added to evidence.
