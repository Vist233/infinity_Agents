# PAPER-FIX-02 secret and diff scan

The changed-scope scan is intentionally limited to the files changed by this
card and its evidence.  Secret values were not read or emitted.

- Scan command:
  `rg -n -i --pcre2 -- "(-----BEGIN (RSA|EC|OPENSSH|PRIVATE) KEY-----|https?://[^[:space:]\"']+:[^[:space:]\"']+@|Bearer[[:space:]]+[A-Za-z0-9._~+/=-]{20,})"`
  over the explicit changed source, test, document, and evidence paths.
- Raw scan exit: `1` (no matches); normalized result: `PASS`.
- `git diff --check` before staging evidence: exit `0`.
- Staged `git diff --cached --check`: exit `0`.
- The same explicit scan was rerun over the staged changed-scope paths after
  staging; raw exit `1` (no matches), normalized result: `PASS`.
- No API key, token, shared secret, private key, credential-bearing URL,
  cookie, or authorization value was read, copied, printed, or committed.
