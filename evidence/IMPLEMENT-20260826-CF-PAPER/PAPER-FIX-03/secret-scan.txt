# PAPER-FIX-03 secret and diff scan

The changed-scope scan is intentionally limited to the files changed by this
card and its evidence.  Secret values were not read or emitted.

- Scan command:
  `rg -n -i --pcre2 -- "(-----BEGIN (RSA|EC|OPENSSH|PRIVATE) KEY-----|https?://[^[:space:]\"']+:[^[:space:]\"']+@|Bearer[[:space:]]+[A-Za-z0-9._~+/=-]{20,})"`
  over the explicit changed source, test, document, and evidence paths.
- The explicit raw scan returned exit `1` with no matches; normalized result:
  `PASS`.
- `git diff --check` after evidence creation returned exit `0`.
- `git diff --cached --check` after staging returned exit `0`.
- The staged explicit raw scan returned exit `1` with no matches; normalized
  result: `PASS`.
- After the checkpoint amendment was added, the final staged scan over the
  complete card scope again returned exit `1` with no matches; normalized
  result: `PASS`, and final staged `git diff --cached --check` remained exit
  `0`.
- No API key, token, shared secret, private key, cookie, authorization value,
  credential-bearing URL, or environment-file content was read, copied,
  printed, or committed.
