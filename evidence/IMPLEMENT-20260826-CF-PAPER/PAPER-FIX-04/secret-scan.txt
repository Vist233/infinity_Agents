# PAPER-FIX-04 secret and diff scan

The scan is limited to this card's changed source, test, document, and
evidence paths. Secret values were not read or emitted.

- Changed-scope raw scan command:
  `rg -n -i --pcre2 -- "(-----BEGIN (RSA|EC|OPENSSH|PRIVATE) KEY-----|https?://[^[:space:]\"']+:[^[:space:]\"']+@|Bearer[[:space:]]+[A-Za-z0-9._~+/=-]{20,})"`
  over the explicit changed paths, excluding this `secret-scan.txt` record.
- Changed-scope raw scan returned exit `1` with no matches; normalized gate:
  `PASS`.
- `git diff --check` after evidence creation returned exit `0`.
- Staged explicit raw scan returned exit `1` with no matches; normalized gate:
  `PASS`.
- Staged `git diff --cached --check` returned exit `0`.
- No API key, token, shared secret, private key, cookie, authorization value,
  credential-bearing URL, or environment-file content was read, copied,
  printed, or committed.
