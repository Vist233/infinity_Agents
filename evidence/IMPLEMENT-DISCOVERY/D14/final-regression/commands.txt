eval "$(pyenv init - zsh)"; pyenv shell Agent; pytest -q
cd cloudflare-worker && npm test
cd cloudflare-worker && npm run check
cd frontend && npm run test:unit
cd frontend && npm run build && npm run typecheck
cd frontend && npm run lint
cd frontend && npx playwright test --config=playwright.local.config.ts
read-only HTTPS, D1, R2, SSH/Docker, and secret-scan verification commands
