Status: PASS.

Archive traversal, symlink, expansion, compression-ratio, malformed-input,
wide-input, and timeout negative paths pass. The production UCI CSV was
reprocessed by the rebuilt Windows Processor and its persisted profile matches
the expected red-wine shape.

Fix commit: 008b905.
